---
phase: 01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/token_seq_generator.h, src/token_seq_generator.cpp, tests/test_token_seq_generator.cpp]
autonomous: true
requirements: [CTRL-03]
must_haves:
  truths:
    - "序列号生成器可以生成全局单调递增的序列号"
    - "序列号生成器线程安全，无竞态条件"
    - "序列号从 0 开始初始化"
    - "序列号递增操作原子性"
  artifacts:
    - path: "src/token_seq_generator.h"
      provides: "序列号生成器接口"
      contains: "get_next_token_seq, get_current_token_seq"
    - path: "src/token_seq_generator.cpp"
      provides: "序列号生成器实现"
      exports: ["g_token_seq_num", "get_next_token_seq"]
    - path: "tests/test_token_seq_generator.cpp"
      provides: "序列号生成器验证"
      min_lines: 60
  key_links:
    - from: "Token 发射函数"
      to: "get_next_token_seq()"
      via: "生成序列号"
      pattern: "get_next_token_seq"
    - from: "接收端去重"
      to: "get_current_token_seq()"
      via: "获取当前序列号"
      pattern: "get_current_token_seq"
---

<objective>
实现全局单调递增的序列号生成器，确保线程安全和原子性，为 Token 帧提供唯一的序列号。

Purpose: 防止 RX 缓冲区旧包诈尸，接收端通过序列号去重。
Output: 序列号生成器头文件、实现文件和单元测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-base-framework/01-CONTEXT.md
@.planning/research/STACK.md

# 现有代码接口
From src/mac_token.h:
```cpp
#pragma pack(push, 1)
struct TokenFrame {
    uint8_t  magic;       // 魔数（0x02）
    uint8_t  target_node; // 目标节点编号
    uint16_t duration_ms; // 授权发射时间片
    uint32_t seq_num;     // 全局单调递增序号
};
#pragma pack(pop)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义序列号生成器接口</name>
  <files>src/token_seq_generator.h</files>
  <behavior>
    - Test 1: get_next_token_seq() 返回递增的序列号
    - Test 2: 序列号从 0 开始
    - Test 3: 多次调用序列号严格递增
  </behavior>
  <action>
在 src/token_seq_generator.h 中定义：

1. 包含必要的头文件：
   ```cpp
   #pragma once
   #include <atomic>
   #include <cstdint>
   ```

2. 定义全局原子变量（per D-01, D-03）：
   ```cpp
   // 全局序列号生成器，线程安全，从 0 开始
   extern std::atomic<uint32_t> g_token_seq_num;
   ```

3. 定义序列号生成函数：
   ```cpp
   // 获取下一个序列号（原子递增）
   uint32_t get_next_token_seq();

   // 获取当前序列号（不递增）
   uint32_t get_current_token_seq();
   ```

注意：
- 使用 `std::atomic<uint32_t>` 确保线程安全（per D-01）
- 使用 `extern` 声明全局变量，在 .cpp 文件中定义
- 序列号类型为 uint32_t，与 TokenFrame.seq_num 一致
- 序列号从 0 开始初始化（per D-02）
  </action>
  <verify>
    <automated>g++ -c src/token_seq_generator.h -o /tmp/test_token_seq.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>头文件存在，包含全局变量声明和函数声明，编译无错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现序列号生成器函数</name>
  <files>src/token_seq_generator.cpp</files>
  <behavior>
    - Test 1: 初始序列号为 0
    - Test 2: get_next_token_seq() 原子递增并返回新值
    - Test 3: 多线程并发调用无竞态条件
  </behavior>
  <action>
在 src/token_seq_generator.cpp 中实现：

1. 定义全局原子变量（per D-02, D-03）：
   ```cpp
   #include "token_seq_generator.h"

   // 全局序列号，从 0 开始初始化
   std::atomic<uint32_t> g_token_seq_num{0};
   ```

2. 实现序列号生成函数（per D-03）：
   ```cpp
   uint32_t get_next_token_seq() {
       // 原子递增并返回新值
       // fetch_add(1) 返回旧值，加 1 得到新值
       return g_token_seq_num.fetch_add(1) + 1;
   }
   ```

3. 实现当前序列号获取函数：
   ```cpp
   uint32_t get_current_token_seq() {
       // 获取当前序列号（原子读取，不递增）
       return g_token_seq_num.load();
   }
   ```

注意：
- 使用 `fetch_add(1)` 实现原子递增（per D-03）
- `fetch_add(1)` 返回旧值，需要加 1 得到新值
- 使用 `load()` 原子读取当前序列号
- 序列号从 0 开始，第一次调用返回 1
- uint32_t 范围足够大（42 亿），极少发生溢出
  </action>
  <verify>
    <automated>./tests/test_token_seq_generator 2>&1 | grep -E "PASS|FAIL" || echo "测试未找到，需先编译"</automated>
  </verify>
  <done>
函数实现完成，满足以下条件：
1. 全局序列号从 0 开始
2. get_next_token_seq() 原子递增并返回新值
3. get_current_token_seq() 返回当前序列号
4. 线程安全，无竞态条件
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 编写序列号生成器单元测试</name>
  <files>tests/test_token_seq_generator.cpp</files>
  <behavior>
    - Test 1: 初始序列号为 0
    - Test 2: 序列号严格递增
    - Test 3: 多线程并发调用无竞态条件
    - Test 4: 序列号不重复
  </behavior>
  <action>
创建 tests/test_token_seq_generator.cpp：

1. 包含必要的头文件：
   ```cpp
   #include <cassert>
   #include <iostream>
   #include <thread>
   #include <vector>
   #include <set>
   #include "../src/token_seq_generator.h"
   ```

2. 编写测试函数：
   ```cpp
   void test_initial_seq() {
       // 验证初始序列号为 0
       assert(get_current_token_seq() == 0);

       std::cout << "PASS: 初始序列号为 0" << std::endl;
   }

   void test_seq_increment() {
       // 验证序列号递增
       uint32_t seq1 = get_next_token_seq();
       uint32_t seq2 = get_next_token_seq();
       uint32_t seq3 = get_next_token_seq();

       assert(seq2 == seq1 + 1);
       assert(seq3 == seq2 + 1);

       std::cout << "PASS: 序列号严格递增" << std::endl;
   }

   void test_thread_safety() {
       // 多线程并发测试
       const int num_threads = 10;
       const int ops_per_thread = 1000;
       std::vector<std::thread> threads;
       std::vector<std::set<uint32_t>> results(num_threads);

       for (int i = 0; i < num_threads; ++i) {
           threads.emplace_back([&, i]() {
               for (int j = 0; j < ops_per_thread; ++j) {
                   uint32_t seq = get_next_token_seq();
                   results[i].insert(seq);
               }
           });
       }

       for (auto& t : threads) {
           t.join();
       }

       // 验证所有序列号唯一（无重复）
       std::set<uint32_t> all_seqs;
       for (const auto& s : results) {
           all_seqs.insert(s.begin(), s.end());
       }

       assert(all_seqs.size() == num_threads * ops_per_thread);

       std::cout << "PASS: 多线程并发无竞态条件" << std::endl;
   }

   int main() {
       test_initial_seq();
       test_seq_increment();
       test_thread_safety();

       std::cout << "所有序列号生成器测试通过！" << std::endl;
       return 0;
   }
   ```

注意：
- 测试初始序列号为 0（per D-02）
- 测试序列号严格递增，无跳跃
- 测试多线程并发，验证无竞态条件
- 使用 std::set 验证序列号唯一性
  </action>
  <verify>
    <automated>g++ -std=c++11 -pthread tests/test_token_seq_generator.cpp src/token_seq_generator.cpp -o tests/test_token_seq_generator && ./tests/test_token_seq_generator</automated>
  </verify>
  <done>单元测试文件存在，所有测试通过（PASS 输出），验证线程安全和原子性</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`make test_token_seq_generator`
2. 单元测试通过：`./tests/test_token_seq_generator` 输出所有 PASS
3. 代码符合规范：
   - 使用 std::atomic 确保线程安全
   - 序列号从 0 开始初始化
   - 使用 fetch_add 实现原子递增
4. Git 提交：`git commit -m "feat(01-02): 实现全局单调递增序列号生成器"`
</verification>

<success_criteria>
1. 序列号生成器头文件存在，包含全局变量声明和函数声明
2. 序列号生成器实现文件存在，正确实现原子递增
3. 全局序列号从 0 开始初始化（per D-02）
4. 序列号递增操作原子性（per D-01, D-03）
5. 多线程并发测试通过，无竞态条件
6. 单元测试通过，验证正确性和线程安全性
7. 代码符合 Google C++ Style Guide
8. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-framework/01-02-SUMMARY.md`
</output>
