---
phase: 01
plan: 03
type: execute
wave: 2
depends_on: [01-00]
files_modified: [src/tx.hpp, src/tx.cpp, tests/test_bypass_fec.cpp]
autonomous: true
requirements: [CTRL-05]
must_haves:
  truths:
    - "Transmitter::send_packet() 支持 bypass_fec 参数"
    - "bypass_fec=true 时跳过 FEC 编码逻辑"
    - "bypass_fec=false 时正常 FEC 编码"
    - "默认参数 bypass_fec=false 保持向后兼容"
  artifacts:
    - path: "src/tx.hpp"
      provides: "Transmitter 类接口（修改）"
      contains: "send_packet with bypass_fec parameter"
    - path: "src/tx.cpp"
      provides: "Transmitter 类实现（修改）"
      contains: "FEC 跳过逻辑"
    - path: "tests/test_bypass_fec.cpp"
      provides: "bypass_fec 功能验证"
      min_lines: 60
  key_links:
    - from: "控制帧发射"
      to: "send_packet(..., bypass_fec=true)"
      via: "跳过 FEC 编码"
      pattern: "send_packet.*bypass_fec"
    - from: "数据帧发射"
      to: "send_packet(..., bypass_fec=false)"
      via: "正常 FEC 编码"
      pattern: "send_packet"
---

<objective>
修改 Transmitter::send_packet() 函数添加 bypass_fec 参数，支持控制帧跳过 FEC 编码逻辑。

Purpose: 控制帧跳过 FEC 编码避免延迟积压（per P1-6），确保控制帧快速传输。
Output: 修改后的发射函数和单元测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-base-framework/01-CONTEXT.md

# 现有代码接口
From src/tx.hpp:
```cpp
class Transmitter {
public:
    bool send_packet(const uint8_t *buf, size_t size, uint8_t flags);
protected:
    virtual void inject_packet(const uint8_t *buf, size_t size) = 0;
};
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修改 Transmitter::send_packet() 添加 bypass_fec 参数</name>
  <files>src/tx.hpp</files>
  <behavior>
    - Test 1: send_packet() 接受 bypass_fec 参数（默认 false）
    - Test 2: 函数声明编译通过
    - Test 3: 保持向后兼容
  </behavior>
  <action>
修改 src/tx.hpp：

1. 修改 send_packet() 函数声明（per D-08）：
   ```cpp
   class Transmitter {
   public:
       Transmitter(int k, int n, const std::string &keypair, uint64_t epoch,
                   uint32_t channel_id, uint32_t fec_delay,
                   std::vector<tags_item_t> &tags);
       virtual ~Transmitter();

       // 添加 bypass_fec 参数（默认 false，保持向后兼容）
       bool send_packet(const uint8_t *buf, size_t size, uint8_t flags,
                       bool bypass_fec = false);

       // ... 其他函数保持不变
   };
   ```

注意：
- 使用默认参数 `bypass_fec = false`，保持向后兼容（per D-08）
- 现有代码无需修改，默认行为不变
- 控制帧发射时传入 `bypass_fec=true`
- 数据帧发射时使用默认值 `bypass_fec=false`
  </action>
  <verify>
    <automated>grep -n "bool bypass_fec" src/tx.hpp && echo "参数添加成功" || echo "参数未添加"</automated>
  </verify>
  <done>
函数声明修改完成，满足以下条件：
1. send_packet() 添加 bypass_fec 参数（默认 false）
2. 编译通过，无语法错误
3. 保持向后兼容
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 FEC 跳过逻辑</name>
  <files>src/tx.cpp</files>
  <behavior>
    - Test 1: bypass_fec=true 时跳过 FEC 编码，直接调用 inject_packet
    - Test 2: bypass_fec=false 时正常执行 FEC 编码逻辑
    - Test 3: 跳过 FEC 时仍执行加密和 Radiotap 封装
  </behavior>
  <action>
修改 src/tx.cpp：

在 send_packet() 函数实现开头添加 FEC 跳过逻辑（per D-08, D-09）：
```cpp
bool Transmitter::send_packet(const uint8_t *buf, size_t size,
                               uint8_t flags, bool bypass_fec) {
    // 如果 bypass_fec=true，跳过 FEC 编码，直接调用 inject_packet
    if (bypass_fec) {
        // 控制帧跳过 FEC 编码逻辑（per D-09）
        // 复用现有的加密和 Radiotap 封装逻辑，仅跳过 FEC

        // 具体实现步骤：
        // 1. 如果启用加密，执行加密操作
        // 2. 添加 Radiotap 头（如果需要）
        // 3. 直接调用 inject_packet()，跳过 FEC 编码

        inject_packet(buf, size);
        return true;
    }

    // 原有的 FEC 编码逻辑（保持不变）
    // ... (现有 FEC 编码实现)
}
```

注意：
- 跳过 FEC 时仍需执行加密和 Radiotap 封装（per D-09）
- 直接调用 inject_packet()，绕过 FEC 编码步骤
- 保持原有 FEC 编码逻辑不变
- 控制帧使用此路径（bypass_fec=true）避免延迟积压（per P1-6）
  </action>
  <verify>
    <automated>grep -A 10 "if (bypass_fec)" src/tx.cpp | grep "inject_packet" && echo "FEC 跳过逻辑实现" || echo "逻辑未实现"</automated>
  </verify>
  <done>
函数实现修改完成，满足以下条件：
1. bypass_fec=true 时跳过 FEC 编码
2. bypass_fec=false 时正常 FEC 编码
3. 跳过 FEC 时仍执行加密和 Radiotap 封装
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 编写 bypass_fec 功能测试</name>
  <files>tests/test_bypass_fec.cpp</files>
  <behavior>
    - Test 1: send_packet(..., bypass_fec=true) 跳过 FEC 编码
    - Test 2: send_packet(..., bypass_fec=false) 正常 FEC 编码
    - Test 3: 默认参数 bypass_fec=false 向后兼容
  </behavior>
  <action>
创建 tests/test_bypass_fec.cpp：

1. 包含必要的头文件：
   ```cpp
   #include <cassert>
   #include <iostream>
   #include "../src/tx.hpp"
   ```

2. 创建 Mock Transmitter 类：
   ```cpp
   // Mock Transmitter 用于测试
   class MockTransmitter : public Transmitter {
   public:
       int inject_count = 0;
       bool last_bypass_fec = false;

       MockTransmitter() : Transmitter(1, 1, "", 0, 0, 0, dummy_tags) {}

       void inject_packet(const uint8_t *buf, size_t size) override {
           inject_count++;
       }

       bool send_packet(const uint8_t *buf, size_t size, uint8_t flags,
                       bool bypass_fec = false) override {
           last_bypass_fec = bypass_fec;
           return Transmitter::send_packet(buf, size, flags, bypass_fec);
       }

       void select_output(int idx) override {}
       void dump_stats(uint64_t ts, uint32_t &inj, uint32_t &drop,
                       uint32_t &bytes) override {}
       void update_radiotap_header(radiotap_header_t &rh) override {}
       radiotap_header_t get_radiotap_header() override { return {}; }
       void set_mark(uint32_t idx) override {}

   private:
       static std::vector<tags_item_t> dummy_tags;
   };
   std::vector<tags_item_t> MockTransmitter::dummy_tags;
   ```

3. 编写测试函数：
   ```cpp
   void test_bypass_fec_true() {
       MockTransmitter tx;
       uint8_t data[] = {0x01, 0x02, 0x03};

       // 测试 bypass_fec=true
       tx.send_packet(data, sizeof(data), 0, true);

       assert(tx.last_bypass_fec == true);
       assert(tx.inject_count > 0);

       std::cout << "PASS: bypass_fec=true 跳过 FEC 编码" << std::endl;
   }

   void test_bypass_fec_false() {
       MockTransmitter tx;
       uint8_t data[] = {0x01, 0x02, 0x03};

       // 测试 bypass_fec=false
       tx.send_packet(data, sizeof(data), 0, false);

       assert(tx.last_bypass_fec == false);

       std::cout << "PASS: bypass_fec=false 正常 FEC 编码" << std::endl;
   }

   void test_default_parameter() {
       MockTransmitter tx;
       uint8_t data[] = {0x01, 0x02, 0x03};

       // 测试默认参数（向后兼容）
       tx.send_packet(data, sizeof(data), 0);  // 不传 bypass_fec 参数

       assert(tx.last_bypass_fec == false);  // 默认为 false

       std::cout << "PASS: 默认参数 bypass_fec=false 向后兼容" << std::endl;
   }

   int main() {
       test_bypass_fec_true();
       test_bypass_fec_false();
       test_default_parameter();

       std::cout << "所有 bypass_fec 测试通过！" << std::endl;
       return 0;
   }
   ```

注意：
- 使用 Mock Transmitter 模拟发射行为
- 验证 bypass_fec=true 跳过 FEC 编码
- 验证 bypass_fec=false 正常 FEC 编码
- 验证默认参数向后兼容
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_bypass_fec.cpp src/tx.cpp -o tests/test_bypass_fec -lpcap && ./tests/test_bypass_fec</automated>
  </verify>
  <done>单元测试文件存在，所有测试通过（PASS 输出），验证 bypass_fec 功能</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`make test_bypass_fec`
2. 单元测试通过：`./tests/test_bypass_fec` 输出所有 PASS
3. 代码符合规范：
   - send_packet() 添加 bypass_fec 参数，保持向后兼容（per D-08）
   - bypass_fec=true 时跳过 FEC 编码（per D-09）
   - 控制帧跳过 FEC 编码避免延迟积压（per P1-6）
4. Git 提交：`git commit -m "feat(01-03): 修改 send_packet 支持 bypass_fec 参数"`
</verification>

<success_criteria>
1. Transmitter::send_packet() 添加 bypass_fec 参数（默认 false）
2. bypass_fec=true 时跳过 FEC 编码逻辑
3. bypass_fec=false 时正常 FEC 编码
4. 保持向后兼容，现有代码无需修改
5. 跳过 FEC 时仍执行加密和 Radiotap 封装
6. 单元测试通过，验证功能正确性
7. 代码符合 Google C++ Style Guide
8. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-framework/01-03-SUMMARY.md`
</output>
