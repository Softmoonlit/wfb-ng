---
phase: 01
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [src/token_emitter.h, src/token_emitter.cpp, src/tx.hpp, src/tx.cpp, tests/test_token_emitter.cpp]
autonomous: true
requirements: [CTRL-05, CTRL-06]
must_haves:
  truths:
    - "控制帧发射时可以跳过 FEC 编码"
    - "Token 帧可以三连发击穿"
    - "接收端可以通过序列号去重"
    - "三连发使用相同的序列号"
  artifacts:
    - path: "src/token_emitter.h"
      provides: "Token 发射器接口"
      contains: "send_token_triple, is_duplicate_token"
    - path: "src/token_emitter.cpp"
      provides: "Token 发射器实现"
      exports: ["send_token_triple", "is_duplicate_token"]
    - path: "src/tx.hpp"
      provides: "Transmitter 类接口（修改）"
      contains: "send_packet with bypass_fec parameter"
    - path: "src/tx.cpp"
      provides: "Transmitter 类实现（修改）"
      contains: "FEC 跳过逻辑"
    - path: "tests/test_token_emitter.cpp"
      provides: "Token 发射器验证"
      min_lines: 80
  key_links:
    - from: "send_token_triple()"
      to: "get_next_token_seq()"
      via: "生成序列号"
      pattern: "get_next_token_seq"
    - from: "send_token_triple()"
      to: "get_radiotap_template()"
      via: "获取控制帧模板"
      pattern: "get_radiotap_template"
    - from: "send_token_triple()"
      to: "Transmitter::send_packet()"
      via: "发射控制帧（bypass_fec=true）"
      pattern: "send_packet"
    - from: "接收端去重"
      to: "is_duplicate_token()"
      via: "序列号去重"
      pattern: "is_duplicate_token"
---

<objective>
实现控制帧发射器，包括 FEC 跳过逻辑和三连发击穿机制，确保控制帧快速可靠传输。

Purpose: 控制帧跳过 FEC 编码避免延迟积压，三连发击穿提高可靠性，接收端通过序列号去重。
Output: Token 发射器头文件、实现文件、修改发射函数和单元测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-base-framework/01-CONTEXT.md
@.planning/research/STACK.md

# 现有代码接口
From src/tx.hpp:
```cpp
class Transmitter {
public:
    bool send_packet(const uint8_t *buf, size_t size, uint8_t flags);
protected:
    virtual void inject_packet(const uint8_t *buf, size_t size) = 0;
};
```

From src/mac_token.h:
```cpp
struct TokenFrame {
    uint8_t  magic;
    uint8_t  target_node;
    uint16_t duration_ms;
    uint32_t seq_num;
};

void serialize_token(const TokenFrame& token, uint8_t* buffer);
TokenFrame parse_token(const uint8_t* buffer);
```

From src/token_seq_generator.h:
```cpp
uint32_t get_next_token_seq();
uint32_t get_current_token_seq();
```

From src/radiotap_template.h:
```cpp
radiotap_header_t* get_radiotap_template(bool is_control_frame);
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修改 Transmitter::send_packet() 添加 bypass_fec 参数</name>
  <files>src/tx.hpp, src/tx.cpp</files>
  <behavior>
    - Test 1: send_packet(..., bypass_fec=true) 跳过 FEC 编码
    - Test 2: send_packet(..., bypass_fec=false) 正常 FEC 编码
    - Test 3: 控制帧发射时 bypass_fec=true
  </behavior>
  <action>
修改 src/tx.hpp 和 src/tx.cpp：

1. 在 src/tx.hpp 中修改函数声明（per D-08）：
   ```cpp
   class Transmitter {
   public:
       Transmitter(int k, int n, const std::string &keypair, uint64_t epoch,
                   uint32_t channel_id, uint32_t fec_delay,
                   std::vector<tags_item_t> &tags);
       virtual ~Transmitter();

       // 添加 bypass_fec 参数（默认 false）
       bool send_packet(const uint8_t *buf, size_t size, uint8_t flags,
                       bool bypass_fec = false);
       // ... 其他函数
   };
   ```

2. 在 src/tx.cpp 中修改函数实现（per D-08, D-09）：
   ```cpp
   bool Transmitter::send_packet(const uint8_t *buf, size_t size,
                                  uint8_t flags, bool bypass_fec) {
       // 如果 bypass_fec=true，跳过 FEC 编码，直接调用 inject_packet
       if (bypass_fec) {
           // 控制帧跳过 FEC 编码逻辑（per D-09）
           // 复用现有的加密和 Radiotap 封装逻辑，仅跳过 FEC
           // 具体实现：
           // 1. 加密（如果启用）
           // 2. 添加 Radiotap 头
           // 3. 直接调用 inject_packet()
           inject_packet(buf, size);
           return true;
       }

       // 原有的 FEC 编码逻辑
       // ... (保持不变)
   }
   ```

注意：
- 使用默认参数 `bypass_fec = false`，保持向后兼容（per D-08）
- 控制帧发射时传入 `bypass_fec=true`
- 数据帧发射时传入 `bypass_fec=false`（或使用默认值）
- 跳过 FEC 时仍需执行加密和 Radiotap 封装（per D-09）
  </action>
  <verify>
    <automated>grep -n "bool bypass_fec" src/tx.hpp && echo "参数添加成功" || echo "参数未添加"</automated>
  </verify>
  <done>
函数修改完成，满足以下条件：
1. send_packet() 添加 bypass_fec 参数（默认 false）
2. bypass_fec=true 时跳过 FEC 编码
3. bypass_fec=false 时正常 FEC 编码
4. 保持向后兼容，现有代码无需修改
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Token 三连发发射器</name>
  <files>src/token_emitter.h, src/token_emitter.cpp</files>
  <behavior>
    - Test 1: send_token_triple() 连续调用 3 次 inject_packet
    - Test 2: 三次发送使用相同的序列号
    - Test 3: 三次发送立即连续，无延迟间隔
    - Test 4: 使用控制帧 Radiotap 模板
  </behavior>
  <action>
创建 src/token_emitter.h 和 src/token_emitter.cpp：

1. 在 src/token_emitter.h 中定义接口：
   ```cpp
   #pragma once
   #include "mac_token.h"
   #include "tx.hpp"

   // Token 三连发发射器
   // 参数:
   //   transmitter: 发射器实例
   //   target_node: 目标节点编号
   //   duration_ms: 授权发射时间片（毫秒）
   void send_token_triple(Transmitter* transmitter,
                          uint8_t target_node,
                          uint16_t duration_ms);

   // 接收端序列号去重
   // 参数:
   //   seq_num: 接收到的序列号
   //   last_seq: 上一次接收的序列号（引用，会被更新）
   // 返回: true 表示重复，false 表示新包
   bool is_duplicate_token(uint32_t seq_num, uint32_t& last_seq);
   ```

2. 在 src/token_emitter.cpp 中实现（per D-11, D-12, D-13）：
   ```cpp
   #include "token_emitter.h"
   #include "token_seq_generator.h"
   #include "radiotap_template.h"
   #include <cstring>

   void send_token_triple(Transmitter* transmitter,
                          uint8_t target_node,
                          uint16_t duration_ms) {
       // 生成全局单调递增的序列号
       uint32_t seq_num = get_next_token_seq();

       // 构建 TokenFrame
       TokenFrame token;
       token.magic = 0x02;            // 控制帧魔数
       token.target_node = target_node;
       token.duration_ms = duration_ms;
       token.seq_num = seq_num;

       // 序列化
       uint8_t buffer[sizeof(TokenFrame)];
       serialize_token(token, buffer);

       // 三连发击穿机制（per D-11, D-12, D-13）
       // 三次发送使用相同的序列号，接收端自动去重
       // 三次发送立即连续，无延迟间隔
       for (int i = 0; i < 3; ++i) {
           transmitter->send_packet(buffer, sizeof(TokenFrame),
                                   /*flags=*/0, /*bypass_fec=*/true);
       }
   }

   bool is_duplicate_token(uint32_t seq_num, uint32_t& last_seq) {
       // 接收端去重逻辑（per D-17, D-18）
       // 新包的 seq_num <= last_seq 时丢弃
       if (seq_num <= last_seq) {
           return true;  // 重复包
       }
       last_seq = seq_num;  // 更新最后接收的序列号
       return false;  // 新包
   }
   ```

注意：
- 三连发在函数内部循环调用 3 次（per D-11）
- 三次发送使用相同的序列号（per D-12）
- 三次发送立即连续，无延迟间隔（per D-13）
- 使用 `bypass_fec=true` 跳过 FEC 编码（per D-10）
- 接收端通过序列号自动去重（per D-17, D-18）
  </action>
  <verify>
    <automated>./tests/test_token_emitter 2>&1 | grep -E "PASS|FAIL" || echo "测试未找到，需先编译"</automated>
  </verify>
  <done>
函数实现完成，满足以下条件：
1. send_token_triple() 连续调用 3 次 send_packet
2. 三次发送使用相同的序列号
3. 三次发送立即连续，无延迟
4. 接收端去重函数正确实现
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 编写 Token 发射器单元测试</name>
  <files>tests/test_token_emitter.cpp</files>
  <behavior>
    - Test 1: send_token_triple() 调用 send_packet 3 次
    - Test 2: 三次发送使用相同的序列号
    - Test 3: 接收端去重函数正确识别重复包
    - Test 4: bypass_fec=true 跳过 FEC 编码
  </behavior>
  <action>
创建 tests/test_token_emitter.cpp：

1. 包含必要的头文件：
   ```cpp
   #include <cassert>
   #include <iostream>
   #include <vector>
   #include "../src/token_emitter.h"
   #include "../src/tx.hpp"
   ```

2. 创建 Mock Transmitter 类：
   ```cpp
   // Mock Transmitter 用于测试
   class MockTransmitter : public Transmitter {
   public:
       std::vector<size_t> injected_sizes;
       std::vector<bool> bypass_fec_flags;

       MockTransmitter() : Transmitter(1, 1, "", 0, 0, 0, dummy_tags) {}

       void inject_packet(const uint8_t *buf, size_t size) override {
           injected_sizes.push_back(size);
       }

       bool send_packet(const uint8_t *buf, size_t size, uint8_t flags,
                       bool bypass_fec = false) override {
           bypass_fec_flags.push_back(bypass_fec);
           inject_packet(buf, size);
           return true;
       }

       void select_output(int idx) override {}
       void dump_stats(uint64_t ts, uint32_t &inj, uint32_t &drop,
                       uint32_t &bytes) override {}
       void update_radiotap_header(radiotap_header_t &rh) override {}
       radiotap_header_t get_radiotap_header() override { return {}; }
       void set_mark(uint32_t idx) override {}

   private:
       static std::vector<tags_item_t> dummy_tags;
   };
   std::vector<tags_item_t> MockTransmitter::dummy_tags;
   ```

3. 编写测试函数：
   ```cpp
   void test_triple_send() {
       MockTransmitter tx;
       send_token_triple(&tx, 5, 100);

       // 验证调用了 3 次 send_packet
       assert(tx.injected_sizes.size() == 3);

       // 验证 bypass_fec=true
       assert(tx.bypass_fec_flags.size() == 3);
       for (bool flag : tx.bypass_fec_flags) {
           assert(flag == true);
       }

       std::cout << "PASS: 三连发正确，bypass_fec=true" << std::endl;
   }

   void test_same_seq_num() {
       // 验证三次发送使用相同的序列号
       // （通过检查发射的数据内容）
       // ... 实现细节略

       std::cout << "PASS: 三次发送使用相同序列号" << std::endl;
   }

   void test_deduplication() {
       uint32_t last_seq = 0;

       // 第一个包（seq=1）
       assert(is_duplicate_token(1, last_seq) == false);
       assert(last_seq == 1);

       // 第二个包（seq=2）
       assert(is_duplicate_token(2, last_seq) == false);
       assert(last_seq == 2);

       // 重复包（seq=1）
       assert(is_duplicate_token(1, last_seq) == true);
       assert(last_seq == 2);  // 未更新

       std::cout << "PASS: 去重逻辑正确" << std::endl;
   }

   int main() {
       test_triple_send();
       test_same_seq_num();
       test_deduplication();

       std::cout << "所有 Token 发射器测试通过！" << std::endl;
       return 0;
   }
   ```

注意：
- 使用 Mock Transmitter 模拟发射行为
- 验证三连发调用了 3 次 send_packet
- 验证 bypass_fec=true 跳过 FEC 编码
- 验证接收端去重逻辑正确
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_token_emitter.cpp src/token_emitter.cpp src/token_seq_generator.cpp src/mac_token.cpp src/radiotap_template.cpp -o tests/test_token_emitter -lpcap && ./tests/test_token_emitter</automated>
  </verify>
  <done>单元测试文件存在，所有测试通过（PASS 输出），验证三连发和去重正确性</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`make test_token_emitter`
2. 单元测试通过：`./tests/test_token_emitter` 输出所有 PASS
3. 代码符合规范：
   - send_packet() 添加 bypass_fec 参数，保持向后兼容
   - 三连发使用相同序列号，立即连续无延迟
   - 接收端通过序列号去重
4. Git 提交：`git commit -m "feat(01-03): 实现控制帧三连发发射器"`
</verification>

<success_criteria>
1. Transmitter::send_packet() 添加 bypass_fec 参数（默认 false）
2. bypass_fec=true 时跳过 FEC 编码逻辑
3. Token 三连发发射器正确实现
4. 三次发送使用相同的序列号
5. 三次发送立即连续，无延迟间隔
6. 接收端去重函数正确识别重复包
7. 单元测试通过，验证正确性
8. 代码符合 Google C++ Style Guide
9. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-framework/01-03-SUMMARY.md`
</output>
