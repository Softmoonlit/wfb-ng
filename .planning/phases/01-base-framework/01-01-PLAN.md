---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/radiotap_template.h, src/radiotap_template.cpp, tests/test_radiotap_template.cpp]
autonomous: true
requirements: [CTRL-04, CTRL-08]
must_haves:
  truths:
    - "Radiotap 模板可以正确封装最低 MCS 速率"
    - "双模板分流可以零延迟切换"
    - "控制帧模板使用 MCS 0 + 20MHz 频宽"
    - "数据帧模板基于 CLI 参数动态生成"
  artifacts:
    - path: "src/radiotap_template.h"
      provides: "Radiotap 双模板定义"
      contains: "g_control_radiotap, g_data_radiotap"
    - path: "src/radiotap_template.cpp"
      provides: "模板初始化和切换函数"
      exports: ["init_radiotap_templates", "get_radiotap_template"]
    - path: "tests/test_radiotap_template.cpp"
      provides: "模板正确性验证"
      min_lines: 50
  key_links:
    - from: "src/radiotap_template.cpp"
      to: "src/tx.hpp"
      via: "init_radiotap_header() 函数"
      pattern: "init_radiotap_header"
    - from: "发射循环"
      to: "get_radiotap_template()"
      via: "模板切换"
      pattern: "get_radiotap_template"
---

<objective>
实现 Radiotap 双模板分流系统，包括控制帧最低 MCS 模板和数据帧高阶 MCS 模板，实现发射时零延迟切换。

Purpose: 控制帧使用最低 MCS 速率确保全网覆盖，数据帧使用高阶 MCS 速率提高吞吐量，双模板分流实现零延迟切换。
Output: Radiotap 模板定义文件、实现文件和单元测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-base-framework/01-CONTEXT.md
@.planning/research/STACK.md

# 现有代码接口
From src/tx.hpp:
```cpp
typedef struct {
    std::vector<uint8_t> header;
    uint8_t stbc;
    bool ldpc;
    bool short_gi;
    uint8_t bandwidth;
    uint8_t mcs_index;
    bool vht_mode;
    uint8_t vht_nss;
} radiotap_header_t;

radiotap_header_t init_radiotap_header(uint8_t stbc,
                                       bool ldpc,
                                       bool short_gi,
                                       uint8_t bandwidth,
                                       uint8_t mcs_index,
                                       bool vht_mode,
                                       uint8_t vht_nss);
```

From src/mac_token.h:
```cpp
#pragma pack(push, 1)
struct TokenFrame {
    uint8_t  magic;       // 魔数（0x02）
    uint8_t  target_node; // 目标节点编号
    uint16_t duration_ms; // 授权发射时间片
    uint32_t seq_num;     // 全局单调递增序号
};
#pragma pack(pop)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 Radiotap 双模板全局变量</name>
  <files>src/radiotap_template.h</files>
  <behavior>
    - Test 1: g_control_radiotap 使用 MCS 0, 20MHz
    - Test 2: g_data_radiotap 初始值为空，等待动态初始化
    - Test 3: 模板全局变量可被发射函数访问
  </behavior>
  <action>
在 src/radiotap_template.h 中定义：

1. 包含必要的头文件：
   - `#include "tx.hpp"` （复用 radiotap_header_t 结构）
   - `#include <atomic>` （线程安全访问）

2. 定义全局静态变量（per D-04, D-05, D-06）：
   ```cpp
   // 控制帧模板：固定使用 MCS 0 + 20MHz（理论速率 6Mbps），确保全网覆盖
   extern radiotap_header_t g_control_radiotap;

   // 数据帧模板：基于 CLI 参数动态初始化
   extern radiotap_header_t g_data_radiotap;
   ```

3. 定义模板初始化函数：
   ```cpp
   // 初始化双模板（在进程启动时调用）
   void init_radiotap_templates(uint8_t data_mcs_index, uint8_t data_bandwidth);
   ```

4. 定义模板获取函数：
   ```cpp
   // 根据帧类型返回对应的模板指针（零延迟切换）
   radiotap_header_t* get_radiotap_template(bool is_control_frame);
   ```

注意：
- 使用 `extern` 声明全局变量，在 .cpp 文件中定义
- 模板切换通过指针实现，避免拷贝开销
- 不要在此处初始化模板，初始化逻辑在 .cpp 文件中
  </action>
  <verify>
    <automated>g++ -c src/radiotap_template.h -o /tmp/test_radiotap_template.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>头文件存在，包含全局变量声明和函数声明，编译无错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Radiotap 模板初始化和切换函数</name>
  <files>src/radiotap_template.cpp</files>
  <behavior>
    - Test 1: init_radiotap_templates() 正确初始化控制帧模板（MCS 0, 20MHz）
    - Test 2: init_radiotap_templates() 正确初始化数据帧模板（基于参数）
    - Test 3: get_radiotap_template(true) 返回控制帧模板指针
    - Test 4: get_radiotap_template(false) 返回数据帧模板指针
  </behavior>
  <action>
在 src/radiotap_template.cpp 中实现：

1. 定义全局静态变量（per D-06）：
   ```cpp
   #include "radiotap_template.h"

   // 全局静态变量定义
   radiotap_header_t g_control_radiotap;
   radiotap_header_t g_data_radiotap;
   ```

2. 实现初始化函数（per D-04, D-05, D-06）：
   ```cpp
   void init_radiotap_templates(uint8_t data_mcs_index, uint8_t data_bandwidth) {
       // 初始化控制帧模板：固定使用 MCS 0 + 20MHz
       g_control_radiotap = init_radiotap_header(
           /*stbc=*/0,
           /*ldpc=*/false,
           /*short_gi=*/false,
           /*bandwidth=*/20,      // 20MHz 频宽
           /*mcs_index=*/0,       // MCS 0（理论速率 6Mbps）
           /*vht_mode=*/false,
           /*vht_nss=*/1
       );

       // 初始化数据帧模板：基于 CLI 参数
       g_data_radiotap = init_radiotap_header(
           /*stbc=*/0,
           /*ldpc=*/false,
           /*short_gi=*/true,     // 启用 short GI 提高吞吐量
           /*bandwidth=*/data_bandwidth,
           /*mcs_index=*/data_mcs_index,
           /*vht_mode=*/false,
           /*vht_nss=*/1
       );
   }
   ```

3. 实现模板获取函数（per D-07）：
   ```cpp
   radiotap_header_t* get_radiotap_template(bool is_control_frame) {
       // 根据帧类型返回对应的模板指针（零延迟切换）
       return is_control_frame ? &g_control_radiotap : &g_data_radiotap;
   }
   ```

注意：
- 复用现有的 init_radiotap_header() 函数，不要重新实现
- 控制帧模板参数固定（MCS 0, 20MHz），确保全网覆盖
- 数据帧模板参数来自 CLI，在进程启动时传入
- 模板切换使用指针，避免拷贝开销
  </action>
  <verify>
    <automated>./tests/test_radiotap_template 2>&1 | grep -E "PASS|FAIL" || echo "测试未找到，需先编译"</automated>
  </verify>
  <done>
函数实现完成，满足以下条件：
1. 控制帧模板使用 MCS 0 + 20MHz
2. 数据帧模板基于参数初始化
3. 模板切换通过指针实现，零延迟
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 编写 Radiotap 模板单元测试</name>
  <files>tests/test_radiotap_template.cpp</files>
  <behavior>
    - Test 1: 控制帧模板初始化后包含正确的 MCS 和频宽
    - Test 2: 数据帧模板初始化后包含正确的参数
    - Test 3: 模板切换函数返回正确的指针
    - Test 4: 控制帧模板的 MCS 索引为 0，频宽为 20MHz
  </behavior>
  <action>
创建 tests/test_radiotap_template.cpp：

1. 包含必要的头文件：
   ```cpp
   #include <cassert>
   #include <iostream>
   #include "../src/radiotap_template.h"
   ```

2. 编写测试函数：
   ```cpp
   void test_control_template() {
       // 初始化模板（数据帧使用 MCS 4, 20MHz 作为测试）
       init_radiotap_templates(4, 20);

       // 验证控制帧模板
       radiotap_header_t* ctrl_template = get_radiotap_template(true);
       assert(ctrl_template != nullptr);
       assert(ctrl_template->mcs_index == 0);    // MCS 0
       assert(ctrl_template->bandwidth == 20);    // 20MHz

       std::cout << "PASS: 控制帧模板正确（MCS 0, 20MHz）" << std::endl;
   }

   void test_data_template() {
       // 验证数据帧模板
       radiotap_header_t* data_template = get_radiotap_template(false);
       assert(data_template != nullptr);
       assert(data_template->mcs_index == 4);     // MCS 4（测试参数）
       assert(data_template->bandwidth == 20);    // 20MHz（测试参数）

       std::cout << "PASS: 数据帧模板正确（MCS 4, 20MHz）" << std::endl;
   }

   void test_template_switching() {
       // 验证模板切换
       radiotap_header_t* ctrl = get_radiotap_template(true);
       radiotap_header_t* data = get_radiotap_template(false);
       assert(ctrl != data);  // 不同的模板
       assert(ctrl == &g_control_radiotap);
       assert(data == &g_data_radiotap);

       std::cout << "PASS: 模板切换正确" << std::endl;
   }

   int main() {
       test_control_template();
       test_data_template();
       test_template_switching();

       std::cout << "所有 Radiotap 模板测试通过！" << std::endl;
       return 0;
   }
   ```

注意：
- 使用 assert 进行断言，验证关键属性
- 测试控制帧模板的固定参数（MCS 0, 20MHz）
- 测试数据帧模板的动态参数
- 测试模板切换的正确性
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_radiotap_template.cpp src/radiotap_template.cpp src/tx.cpp -o tests/test_radiotap_template -lpcap && ./tests/test_radiotap_template</automated>
  </verify>
  <done>单元测试文件存在，所有测试通过（PASS 输出），验证模板正确性</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`make test_radiotap_template`
2. 单元测试通过：`./tests/test_radiotap_template` 输出所有 PASS
3. 代码符合规范：
   - 复用现有 init_radiotap_header() 函数
   - 使用指针切换模板，零延迟
   - 控制帧模板固定 MCS 0 + 20MHz
4. Git 提交：`git commit -m "feat(01-01): 实现 Radiotap 双模板分流系统"`
</verification>

<success_criteria>
1. Radiotap 模板头文件存在，包含全局变量声明和函数声明
2. 模板实现文件存在，正确初始化双模板
3. 控制帧模板使用 MCS 0 + 20MHz，确保全网覆盖
4. 数据帧模板基于 CLI 参数动态初始化
5. 模板切换函数返回正确的指针，零延迟
6. 单元测试通过，验证模板正确性
7. 代码符合 Google C++ Style Guide
8. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-framework/01-01-SUMMARY.md`
</output>
