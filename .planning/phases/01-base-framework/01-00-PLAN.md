---
phase: 01
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_mac_token.cpp]
autonomous: true
requirements: [CTRL-01, CTRL-02]
must_haves:
  truths:
    - "TokenFrame 结构体字节对齐无填充"
    - "序列化函数正确编码 TokenFrame"
    - "反序列化函数正确解码 TokenFrame"
    - "序列化和反序列化可以往返转换"
  artifacts:
    - path: "src/mac_token.h"
      provides: "TokenFrame 结构定义（已存在）"
      contains: "struct TokenFrame"
    - path: "src/mac_token.cpp"
      provides: "序列化/反序列化函数（已存在）"
      exports: ["serialize_token", "parse_token"]
    - path: "tests/test_mac_token.cpp"
      provides: "TokenFrame 验证（新增测试）"
      min_lines: 80
  key_links:
    - from: "序列化函数"
      to: "TokenFrame 结构"
      via: "memcpy 序列化"
      pattern: "serialize_token"
    - from: "反序列化函数"
      to: "TokenFrame 结构"
      via: "memcpy 反序列化"
      pattern: "parse_token"
---

<objective>
验证现有 TokenFrame 结构体和序列化/反序列化函数的正确性，确保控制帧基础数据结构符合规范。

Purpose: TokenFrame 是控制帧的核心数据结构，必须确保字节对齐无填充、序列化/反序列化正确，为后续三连发和去重逻辑提供坚实基础。
Output: 验证现有实现正确性的单元测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-base-framework/01-CONTEXT.md

# 现有代码接口
From src/mac_token.h:
```cpp
#pragma pack(push, 1)
struct TokenFrame {
    uint8_t  magic;       // 魔数（0x02）
    uint8_t  target_node; // 目标节点编号
    uint16_t duration_ms; // 授权发射时间片
    uint32_t seq_num;     // 全局单调递增序号
};
#pragma pack(pop)

void serialize_token(const TokenFrame& token, uint8_t* buffer);
TokenFrame parse_token(const uint8_t* buffer);
```

From src/mac_token.cpp:
```cpp
void serialize_token(const TokenFrame& token, uint8_t* buffer) {
    std::memcpy(buffer, &token, sizeof(TokenFrame));
}

TokenFrame parse_token(const uint8_t* buffer) {
    TokenFrame token;
    std::memcpy(&token, buffer, sizeof(TokenFrame));
    return token;
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 验证 TokenFrame 字节对齐</name>
  <files>tests/test_mac_token.cpp</files>
  <behavior>
    - Test 1: sizeof(TokenFrame) 等于 8 字节
    - Test 2: 结构体字段按预期顺序排列
    - Test 3: 无编译器填充字节
  </behavior>
  <action>
创建 tests/test_mac_token.cpp：

1. 包含必要的头文件：
   ```cpp
   #include <cassert>
   #include <iostream>
   #include <cstring>
   #include "../src/mac_token.h"
   ```

2. 编写字节对齐测试：
   ```cpp
   void test_token_frame_size() {
       // 验证 TokenFrame 结构体大小为 8 字节（无填充）
       // 字段布局：
       //   uint8_t  magic       (1 字节)
       //   uint8_t  target_node (1 字节)
       //   uint16_t duration_ms (2 字节)
       //   uint32_t seq_num     (4 字节)
       //   总计：1 + 1 + 2 + 4 = 8 字节

       assert(sizeof(TokenFrame) == 8);

       std::cout << "PASS: TokenFrame 大小为 8 字节（无填充）" << std::endl;
   }

   void test_token_frame_alignment() {
       // 验证字段偏移量正确
       TokenFrame token;

       // 验证字段偏移量（per ROADMAP 成功标准 1）
       assert(offsetof(TokenFrame, magic) == 0);
       assert(offsetof(TokenFrame, target_node) == 1);
       assert(offsetof(TokenFrame, duration_ms) == 2);
       assert(offsetof(TokenFrame, seq_num) == 4);

       std::cout << "PASS: TokenFrame 字段对齐正确" << std::endl;
   }
   ```

注意：
- TokenFrame 使用 `#pragma pack(push, 1)` 确保字节对齐（per CTRL-01）
- 字段布局必须严格按照规范，确保跨平台兼容性
- 使用 offsetof 宏验证字段偏移量
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_mac_token.cpp src/mac_token.cpp -o tests/test_mac_token && ./tests/test_mac_token 2>&1 | grep "PASS: TokenFrame"</automated>
  </verify>
  <done>字节对齐测试存在，验证 TokenFrame 大小为 8 字节，字段偏移正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 验证序列化和反序列化函数</name>
  <files>tests/test_mac_token.cpp</files>
  <behavior>
    - Test 1: 序列化函数正确编码 TokenFrame
    - Test 2: 反序列化函数正确解码 TokenFrame
    - Test 3: 序列化和反序列化可以往返转换（per ROADMAP 成功标准 2）
  </behavior>
  <action>
在 tests/test_mac_token.cpp 中添加：

```cpp
void test_serialize_token() {
    // 创建测试 TokenFrame
    TokenFrame token;
    token.magic = 0x02;
    token.target_node = 5;
    token.duration_ms = 100;
    token.seq_num = 12345;

    // 序列化
    uint8_t buffer[sizeof(TokenFrame)];
    serialize_token(token, buffer);

    // 验证字节序（小端序）
    assert(buffer[0] == 0x02);          // magic
    assert(buffer[1] == 5);             // target_node
    assert(buffer[2] == 100 && buffer[3] == 0);  // duration_ms (小端序)
    // seq_num 字段验证（小端序）
    assert(buffer[4] == 0x39 && buffer[5] == 0x30 &&
           buffer[6] == 0x00 && buffer[7] == 0x00);

    std::cout << "PASS: 序列化函数正确编码 TokenFrame" << std::endl;
}

void test_parse_token() {
    // 创建测试缓冲区（小端序）
    uint8_t buffer[sizeof(TokenFrame)] = {0x02, 5, 100, 0, 0x39, 0x30, 0x00, 0x00};

    // 反序列化
    TokenFrame token = parse_token(buffer);

    // 验证字段值
    assert(token.magic == 0x02);
    assert(token.target_node == 5);
    assert(token.duration_ms == 100);
    assert(token.seq_num == 12345);

    std::cout << "PASS: 反序列化函数正确解码 TokenFrame" << std::endl;
}

void test_round_trip() {
    // 测试往返转换：序列化 -> 反序列化（per ROADMAP 成功标准 2）
    TokenFrame original;
    original.magic = 0x02;
    original.target_node = 10;
    original.duration_ms = 200;
    original.seq_num = 67890;

    // 序列化
    uint8_t buffer[sizeof(TokenFrame)];
    serialize_token(original, buffer);

    // 反序列化
    TokenFrame restored = parse_token(buffer);

    // 验证往返转换正确
    assert(restored.magic == original.magic);
    assert(restored.target_node == original.target_node);
    assert(restored.duration_ms == original.duration_ms);
    assert(restored.seq_num == original.seq_num);

    std::cout << "PASS: 序列化和反序列化可以往返转换" << std::endl;
}
```

注意：
- 序列化使用 memcpy 直接拷贝内存（per CTRL-02）
- 字节序为小端序（x86/ARM 平台）
- 测试往返转换验证序列化/反序列化正确性
  </action>
  <verify>
    <automated>./tests/test_mac_token 2>&1 | grep "PASS: 序列化\|PASS: 反序列化\|PASS: 往返"</automated>
  </verify>
  <done>
测试函数存在，验证以下条件：
1. 序列化函数正确编码 TokenFrame
2. 反序列化函数正确解码 TokenFrame
3. 往返转换正确无损
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 运行完整测试套件</name>
  <files>tests/test_mac_token.cpp</files>
  <behavior>
    - Test 1: 所有测试通过，无断言失败
    - Test 2: 输出清晰的测试结果
  </behavior>
  <action>
在 tests/test_mac_token.cpp 中添加 main 函数：

```cpp
int main() {
    std::cout << "=== TokenFrame 单元测试 ===" << std::endl;

    test_token_frame_size();
    test_token_frame_alignment();
    test_serialize_token();
    test_parse_token();
    test_round_trip();

    std::cout << std::endl;
    std::cout << "所有 TokenFrame 测试通过！" << std::endl;
    return 0;
}
```

确保测试可编译运行：
```bash
g++ -std=c++11 tests/test_mac_token.cpp src/mac_token.cpp -o tests/test_mac_token
./tests/test_mac_token
```

预期输出：
```
=== TokenFrame 单元测试 ===
PASS: TokenFrame 大小为 8 字节（无填充）
PASS: TokenFrame 字段对齐正确
PASS: 序列化函数正确编码 TokenFrame
PASS: 反序列化函数正确解码 TokenFrame
PASS: 序列化和反序列化可以往返转换

所有 TokenFrame 测试通过！
```
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_mac_token.cpp src/mac_token.cpp -o tests/test_mac_token && ./tests/test_mac_token</automated>
  </verify>
  <done>
测试可执行文件存在，所有测试通过，输出清晰，验证 TokenFrame 正确性。
  </done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 tests/test_mac_token.cpp src/mac_token.cpp -o tests/test_mac_token`
2. 单元测试通过：`./tests/test_mac_token` 输出所有 PASS
3. 验证符合 ROADMAP 成功标准 1 和 2：
   - 成功标准 1: TokenFrame 结构体正确定义，字节对齐无填充 ✓
   - 成功标准 2: 序列化/反序列化函数通过单元测试，验证字节正确性 ✓
4. Git 提交：`git commit -m "test(01-00): 验证 TokenFrame 实现正确性"`
</verification>

<success_criteria>
1. TokenFrame 结构体字节对齐无填充，大小为 8 字节
2. 字段偏移量正确：magic=0, target_node=1, duration_ms=2, seq_num=4
3. 序列化函数正确编码 TokenFrame 为字节数组
4. 反序列化函数正确解码字节数组为 TokenFrame
5. 序列化和反序列化可以往返转换，无损无误差
6. 单元测试通过，所有测试输出 PASS
7. 代码符合 Google C++ Style Guide
8. 所有测试代码添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-framework/01-00-SUMMARY.md`
</output>
