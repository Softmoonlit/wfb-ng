---
phase: 05
plan: 02
type: execute
wave: 3
depends_on:
  - 05-00
  - 05-01
files_modified:
  - src/wfb_core.cpp
  - src/tx_worker.h
  - src/tx_worker.cpp
  - src/resource_guard.h
autonomous: true
requirements:
  - PROC-05
  - PROC-07
  - RT-03

must_haves:
  truths:
    - "发射线程正确等待 condition_variable 唤醒（零轮询）"
    - "Token 授权后，在窗口内正确发射数据包"
    - "Token 过期时立即停止发射"
    - "pcap_inject 失败有正确的错误处理和重试"
    - "SCHED_FIFO 90 优先级正确设置"
    - "帧构建内存安全，无缓冲区溢出"
  artifacts:
    - path: "src/tx_worker.h"
      provides: "发射线程接口"
      contains: "tx_main_loop"
    - path: "src/tx_worker.cpp"
      provides: "发射线程实现"
      min_lines: 200
  key_links:
    - from: "src/tx_worker.cpp"
      to: "src/resource_guard.h"
      via: "pcap 句柄管理"
      pattern: "PcapHandle"
    - from: "src/tx_worker.cpp"
      to: "src/threads.h"
      via: "condition_variable 唤醒"
      pattern: "cv_send.wait"
---

# Plan 05-02: TX 发射线程整合（增强版）

<objective>
将 tx.cpp 中的发射逻辑整合到 wfb_core 中，实现 tx_main_loop 负责零轮询唤醒、门控控制、数据包发射。

**评审反馈整合**：
- HIGH: pcap_inject 失败时添加错误处理和重试逻辑
- HIGH: 帧构建使用安全的缓冲区操作，避免溢出
- LOW: 批量发射参数可配置

Purpose: 建立稳健的发射线程，实现零轮询唤醒和门控控制
Output: 生产就绪的发射线程，包含完整错误处理
</objective>

<execution_context>
@/home/kilome/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@/home/kilome/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wfb-core/05-CONTEXT.md
@.planning/phases/05-wfb-core/05-RESEARCH.md

<interfaces>
From src/packet_queue.h:
```cpp
class PacketQueue {
public:
    struct Packet {
        uint8_t data[1450];
        size_t len;
    };
    bool push(const Packet& pkt);
    bool try_pop(Packet& pkt);
    size_t size() const;
    size_t capacity() const;
};
```

From src/radiotap_template.h:
```cpp
class RadiotapTemplate {
public:
    void init_for_data(int mcs, int bandwidth);
    const uint8_t* get_template() const;
    uint16_t get_length() const;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 TX 线程头文件和接口</name>
  <files>src/tx_worker.h</files>
  <behavior>
    - Test 1: tx_main_loop 函数签名验证
    - Test 2: TxConfig 结构体验证
    - Test 3: 错误码定义验证
  </behavior>
  <action>
定义 TX 线程接口，包含错误处理和配置

```cpp
#ifndef TX_WORKER_H
#define TX_WORKER_H

#include "threads.h"
#include <pcap.h>
#include <atomic>
#include <cstdint>

// pcap 前向声明
typedef struct pcap pcap_t;

// TX 线程配置
struct TxConfig {
    int mcs;                    // MCS 调制方案
    int bandwidth;              // 频宽（MHz）
    uint32_t batch_size;        // 批量发射大小（默认 32）
    uint32_t inject_retries;    // pcap_inject 重试次数（默认 3）
    uint32_t inject_retry_delay_us;  // 重试延迟（默认 100us）
};

// TX 线程错误码
enum class TxError {
    OK = 0,
    PCAP_INJECT_FAILED,
    FRAME_BUILD_FAILED,
    THREAD_SHUTDOWN,
    TOKEN_EXPIRED
};

// TX 线程统计
struct TxStats {
    std::atomic<uint64_t> packets_sent{0};
    std::atomic<uint64_t> packets_failed{0};
    std::atomic<uint64_t> inject_retries{0};
    std::atomic<uint64_t> token_expired{0};
};

/**
 * TX 发射线程主函数
 *
 * @param shared_state 线程共享状态
 * @param pcap_handle pcap 句柄
 * @param config TX 配置
 * @param shutdown 关闭信号
 * @param stats 统计信息（可选）
 */
TxError tx_main_loop(
    ThreadSharedState* shared_state,
    pcap_t* pcap_handle,
    const TxConfig& config,
    std::atomic<bool>* shutdown,
    TxStats* stats = nullptr
);

#endif  // TX_WORKER_H
```
  </action>
  <verify>
    <automated>grep -E "tx_main_loop|TxConfig|TxError" src/tx_worker.h && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - TX 线程接口定义完整
    - 错误码定义清晰
    - 配置结构体完整
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现帧构建函数（安全版）</name>
  <files>src/tx_worker.cpp</files>
  <behavior>
    - Test 1: 正常数据帧构建成功
    - Test 2: 空数据包返回错误
    - Test 3: 帧大小在合理范围内
    - Test 4: Radiotap 头部正确追加
  </behavior>
  <action>
实现安全的帧构建函数，避免缓冲区溢出

```cpp
#include "tx_worker.h"
#include "packet_queue.h"
#include "watermark.h"
#include "radiotap_template.h"
#include "guard_interval.h"
#include "error_handler.h"

#include <cstring>
#include <iostream>
#include <vector>
#include <algorithm>

namespace {

// IEEE 802.11 数据帧头部最小长度
constexpr size_t kMinFrameLen = 24;  // 基本数据帧头部
constexpr size_t kMaxFrameLen = 2350;  // 最大帧长（含 Radiotap）

/**
 * 安全构建 IEEE 802.11 数据帧
 * 
 * @param pkt 数据包
 * @param rtap_template Radiotap 模板
 * @param frame 输出帧缓冲区
 * @return 成功返回帧长度，失败返回 0
 */
size_t build_frame_safe(const PacketQueue::Packet& pkt,
                        const RadiotapTemplate& rtap_template,
                        std::vector<uint8_t>& frame) {
    // 参数验证
    if (pkt.len == 0 || pkt.len > 1450) {
        std::cerr << "无效数据包长度: " << pkt.len << std::endl;
        return 0;
    }

    // 清空输出缓冲区
    frame.clear();
    frame.reserve(kMaxFrameLen);

    // Radiotap 头部
    const uint8_t* rtap_data = rtap_template.get_template();
    uint16_t rtap_len = rtap_template.get_length();
    
    if (!rtap_data || rtap_len == 0) {
        std::cerr << "Radiotap 模板未初始化" << std::endl;
        return 0;
    }
    
    frame.insert(frame.end(), rtap_data, rtap_data + rtap_len);

    // IEEE 802.11 数据帧头部
    // Frame Control: Data frame, To DS
    frame.push_back(0x08);  // Data frame
    frame.push_back(0x01);  // To DS
    
    // Duration (2 bytes)
    frame.push_back(0x00);
    frame.push_back(0x00);
    
    // Address 1-3 (各 6 字节)
    // Address 1: BSSID（使用广播地址简化）
    for (int i = 0; i < 6; i++) frame.push_back(0xFF);
    // Address 2: SA（源地址）
    for (int i = 0; i < 6; i++) frame.push_back(0x00);
    // Address 3: DA（目的地址）
    for (int i = 0; i < 6; i++) frame.push_back(0xFF);
    
    // Sequence Control (2 bytes)
    frame.push_back(0x00);
    frame.push_back(0x00);

    // 数据载荷
    frame.insert(frame.end(), pkt.data, pkt.data + pkt.len);

    // 验证帧长度
    if (frame.size() > kMaxFrameLen) {
        std::cerr << "帧过长: " << frame.size() << " bytes" << std::endl;
        return 0;
    }

    return frame.size();
}

/**
 * 带重试的 pcap_inject
 * 
 * @param pcap_handle pcap 句柄
 * @param data 数据指针
 * @param len 数据长度
 * @param max_retries 最大重试次数
 * @param retry_delay_us 重试延迟（微秒）
 * @return 成功返回实际发送字节数，失败返回 -1
 */
int pcap_inject_retry(pcap_t* pcap_handle,
                      const uint8_t* data,
                      size_t len,
                      int max_retries,
                      uint32_t retry_delay_us) {
    for (int retry = 0; retry < max_retries; retry++) {
        int ret = pcap_inject(pcap_handle, data, len);
        if (ret >= 0) {
            return ret;
        }
        
        // 检查是否可重试
        int err = pcap_geterr(pcap_handle) ? errno : 0;
        if (err == EAGAIN || err == EWOULDBLOCK || err == ENOBUFS) {
            // 可重试错误
            if (retry < max_retries - 1) {
                usleep(retry_delay_us);
                continue;
            }
        }
        
        // 不可重试或重试次数用尽
        std::cerr << "pcap_inject 失败 (retry " << retry + 1 << "/" 
                  << max_retries << "): " << pcap_geterr(pcap_handle) << std::endl;
        return -1;
    }
    
    return -1;
}

}  // namespace
```
  </action>
  <verify>
    <automated>grep -E "build_frame_safe|pcap_inject_retry" src/tx_worker.cpp && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 帧构建函数参数验证完整
    - 缓冲区溢出保护
    - pcap_inject 重试逻辑实现
    - 错误日志输出
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 TX 线程主循环</name>
  <files>src/tx_worker.cpp</files>
  <behavior>
    - Test 1: condition_variable 唤醒正确
    - Test 2: Token 授权后发射数据包
    - Test 3: Token 过期时停止发射
    - Test 4: shutdown 信号触发后快速退出
  </behavior>
  <action>
实现完整的 TX 线程主循环，包含门控控制和错误处理

```cpp
TxError tx_main_loop(
    ThreadSharedState* shared_state,
    pcap_t* pcap_handle,
    const TxConfig& config,
    std::atomic<bool>* shutdown,
    TxStats* stats
) {
    // 参数验证
    if (!shared_state || !pcap_handle || !shutdown) {
        return TxError::THREAD_SHUTDOWN;
    }

    // 初始化 Radiotap 模板
    RadiotapTemplate rtap_template;
    rtap_template.init_for_data(config.mcs, config.bandwidth);

    // 批量发射缓冲区
    std::vector<PacketQueue::Packet> batch;
    batch.reserve(config.batch_size);

    // 帧构建缓冲区
    std::vector<uint8_t> frame;
    frame.reserve(kMaxFrameLen);

    std::cout << "TX 线程启动，MCS=" << config.mcs 
              << ", 批量大小=" << config.batch_size << std::endl;

    while (!shutdown->load()) {
        // 零轮询唤醒：等待 can_send 或 shutdown
        std::unique_lock<std::mutex> lock(shared_state->mtx);
        shared_state->cv_send.wait(lock, [&]() {
            return shared_state->can_send || shutdown->load();
        });

        if (shutdown->load()) break;

        // 门控检查
        uint64_t now_ms = get_monotonic_ms();
        if (!shared_state->can_send || 
            now_ms >= shared_state->token_expire_time_ms) {
            // Token 无效或已过期
            shared_state->can_send = false;
            continue;
        }

        // 计算剩余授权时间
        uint64_t remaining_ms = shared_state->token_expire_time_ms - now_ms;

        // 解锁，准备发射
        lock.unlock();

        // 批量取包
        batch.clear();
        PacketQueue::Packet pkt;
        while (batch.size() < config.batch_size && 
               shared_state->packet_queue.try_pop(pkt)) {
            batch.push_back(std::move(pkt));
        }

        // 发射数据包
        for (size_t i = 0; i < batch.size(); i++) {
            // 检查 Token 是否过期
            if (get_monotonic_ms() >= shared_state->token_expire_time_ms) {
                // Token 过期，停止发射
                if (stats) stats->token_expired++;
                break;
            }

            // 构建帧
            size_t frame_len = build_frame_safe(batch[i], rtap_template, frame);
            if (frame_len == 0) {
                if (stats) stats->packets_failed++;
                continue;
            }

            // 发射（带重试）
            int ret = pcap_inject_retry(
                pcap_handle, 
                frame.data(), 
                frame_len,
                config.inject_retries,
                config.inject_retry_delay_us
            );

            if (ret < 0) {
                if (stats) stats->packets_failed++;
                continue;
            }

            if (stats) stats->packets_sent++;
        }

        // 应用保护间隔（微间隔，避免连续发射拥塞）
        apply_guard_interval(1);  // 1ms
    }

    std::cout << "TX 线程正常退出" << std::endl;
    return TxError::OK;
}
```
  </action>
  <verify>
    <automated>make tx_worker && echo "编译通过" || echo "编译失败"</automated>
  </verify>
  <done>
    - TX 线程正确实现
    - 零轮询唤醒（无 usleep）
    - Token 门控正确
    - pcap_inject 带重试
    - 帧构建内存安全
  </done>
</task>

<task type="auto">
  <name>Task 4: 更新 wfb_core.cpp 集成 TX 线程</name>
  <files>src/wfb_core.cpp</files>
  <action>
在主程序中启动 TX 线程，设置实时优先级

```cpp
#include "tx_worker.h"

// 在 run_server() 和 run_client() 中

// TX 配置
TxConfig tx_config = {
    .mcs = config.mcs,
    .bandwidth = config.bandwidth,
    .batch_size = 32,
    .inject_retries = 3,
    .inject_retry_delay_us = 100
};

// 启动 TX 线程
TxStats tx_stats;
std::thread tx_thread([&]() {
    // 设置实时优先级 90
    if (!set_thread_realtime_priority(ThreadPriority::TX_WORKER, "tx_worker")) {
        std::cerr << "警告: 无法设置 TX 线程实时优先级" << std::endl;
    }

    TxError err = tx_main_loop(&shared_state, pcap_handle.get(), 
                               tx_config, &g_shutdown, &tx_stats);

    if (err != TxError::OK) {
        std::cerr << "TX 线程异常退出: " << static_cast<int>(err) << std::endl;
    }
});

// ... 等待所有线程 ...

tx_thread.join();

// 输出统计信息
std::cout << "TX 统计: 发送=" << tx_stats.packets_sent.load()
          << ", 失败=" << tx_stats.packets_failed.load()
          << ", Token过期=" << tx_stats.token_expired.load() << std::endl;
```
  </action>
  <verify>
    <automated>grep -E "tx_main_loop|ThreadPriority::TX_WORKER" src/wfb_core.cpp && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - TX 线程正确启动
    - 实时优先级 90 设置
    - 统计信息输出
    - 与主程序正确集成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 队列 → TX 线程 | 内部数据，可信 |
| TX 线程 → WiFi 空口 | 外部发射，需要错误处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | DoS | pcap_inject | mitigate | 重试机制，错误日志 |
| T-05-07 | Tampering | 帧构建 | mitigate | 参数验证，缓冲区溢出保护 |
</threat_model>

<verification>
- TX 线程编译通过
- condition_variable 唤醒正确
- Token 门控逻辑正确
- pcap_inject 重试机制正常
- 帧构建无内存溢出
</verification>

<success_criteria>
1. 发射线程正确等待 condition_variable 唤醒
2. Token 授权后，在窗口内正确发射数据包
3. Token 过期时立即停止发射
4. pcap_inject 失败有重试逻辑
5. SCHED_FIFO 90 优先级设置
6. 帧构建内存安全
</success_criteria>

<output>
After completion, create `.planning/phases/05-wfb-core/05-02-SUMMARY.md`
</output>
