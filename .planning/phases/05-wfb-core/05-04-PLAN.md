---
phase: 05
plan: 04
type: execute
wave: 4
depends_on:
  - 05-00
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/wfb_core.cpp
  - src/scheduler_worker.h
  - src/scheduler_worker.cpp
autonomous: true
requirements:
  - PROC-09
  - PROC-10
  - SCHED-01
  - SCHED-02

must_haves:
  truths:
    - "调度器线程在 Server 模式下正确运行"
    - "Token 三连发正确执行（击穿机制）"
    - "保护间隔（5ms）在节点切换时正确应用"
    - "所有 Token 发射错误都有处理和日志"
    - "SCHED_FIFO 95 优先级正确设置"
    - "节点状态机跃迁符合 Phase 3 设计"
  artifacts:
    - path: "src/scheduler_worker.h"
      provides: "调度器线程接口"
      contains: "scheduler_main_loop"
    - path: "src/scheduler_worker.cpp"
      provides: "调度器线程实现"
      min_lines: 150
  key_links:
    - from: "src/scheduler_worker.cpp"
      to: "src/server_scheduler.h"
      via: "调度器引擎调用"
      pattern: "get_next_scheduled_node|calculate_time_quota"
    - from: "src/scheduler_worker.cpp"
      to: "src/token_emitter.h"
      via: "Token 三连发"
      pattern: "send_token_triple"
---

# Plan 05-04: 服务端调度器线程整合（增强版）

<objective>
将 Phase 3 实现的调度器引擎整合到 wfb_core 中，实现 scheduler_thread 负责服务端 Token 分配、状态机跃迁、AQ/SQ 队列管理。

**评审反馈整合**：
- HIGH: Token 发射失败添加错误处理和日志
- MEDIUM: 实时优先级反转风险 - 调度器线程优先级 95，低于控制面 99
- LOW: 节点状态机跃迁添加竞争条件防护

Purpose: 建立稳健的服务端调度线程，实现 Token 分配和状态机管理
Output: 生产就绪的调度器线程，包含完整错误处理
</objective>

<execution_context>
@/home/kilome/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@/home/kilome/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wfb-core/05-CONTEXT.md
@.planning/phases/05-wfb-core/05-RESEARCH.md

<interfaces>
From src/server_scheduler.h:
```cpp
class ServerScheduler {
public:
    void register_node(uint8_t node_id);
    uint8_t get_next_scheduled_node();
    uint32_t calculate_time_quota(uint8_t node_id);
    void idle_patrol();
    void update_node_state(uint8_t node_id);
    size_t get_node_count();
    void schedule_node_for_nack(uint8_t node_id, uint32_t time_ms);
};
```

From src/token_emitter.h:
```cpp
class TokenEmitter {
public:
    TokenEmitter(pcap_t* pcap_handle);
    bool send_token_triple(const TokenFrame& token);
    uint32_t get_next_seq();
};
```

From src/breathing_cycle.h:
```cpp
class BreathingCycle {
public:
    void advance_phase();
    bool is_inhale_phase();
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建调度器线程头文件和接口</name>
  <files>src/scheduler_worker.h</files>
  <behavior>
    - Test 1: scheduler_main_loop 函数签名验证
    - Test 2: SchedulerConfig 结构体验证
    - Test 3: 错误码定义验证
  </behavior>
  <action>
定义调度器线程接口，包含错误处理和配置

```cpp
#ifndef SCHEDULER_WORKER_H
#define SCHEDULER_WORKER_H

#include "threads.h"
#include "config.h"
#include <atomic>
#include <pcap.h>
#include <cstdint>

// 前向声明
class ServerScheduler;

// 调度器线程配置
struct SchedulerConfig {
    uint32_t min_window_ms;     // 最小授权窗口（默认 50ms）
    uint32_t max_window_ms;     // 最大授权窗口（默认 300ms）
    uint32_t guard_interval_ms; // 保护间隔（默认 5ms）
    uint8_t node_count;         // 节点数量
};

// 调度器线程错误码
enum class SchedulerError {
    OK = 0,
    TOKEN_SEND_FAILED,
    NO_NODES_REGISTERED,
    THREAD_SHUTDOWN,
    UNKNOWN_ERROR
};

// 调度器线程统计
struct SchedulerStats {
    std::atomic<uint64_t> tokens_sent{0};
    std::atomic<uint64_t> tokens_failed{0};
    std::atomic<uint64_t> idle_patrols{0};
    std::atomic<uint64_t> node_switches{0};
};

/**
 * 调度器线程主函数（仅 Server 模式）
 *
 * @param shared_state 线程共享状态
 * @param scheduler 调度器实例
 * @param pcap_handle pcap 句柄
 * @param config 调度器配置
 * @param shutdown 关闭信号
 * @param stats 统计信息（可选）
 */
SchedulerError scheduler_main_loop(
    ThreadSharedState* shared_state,
    ServerScheduler* scheduler,
    pcap_t* pcap_handle,
    const SchedulerConfig& config,
    std::atomic<bool>* shutdown,
    SchedulerStats* stats = nullptr
);

#endif  // SCHEDULER_WORKER_H
```
  </action>
  <verify>
    <automated>grep -E "scheduler_main_loop|SchedulerConfig|SchedulerError" src/scheduler_worker.h && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 调度器线程接口定义完整
    - 错误码定义清晰
    - 配置结构体完整
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现调度器线程主循环</name>
  <files>src/scheduler_worker.cpp</files>
  <behavior>
    - Test 1: Token 发射正确
    - Test 2: 保护间隔正确应用
    - Test 3: 空闲巡逻正确触发
    - Test 4: shutdown 信号触发后快速退出
    - Test 5: Token 发射失败有错误处理
  </behavior>
  <action>
实现完整的调度器线程，包含 Token 发射和状态机管理

```cpp
#include "scheduler_worker.h"
#include "server_scheduler.h"
#include "mac_token.h"
#include "token_emitter.h"
#include "breathing_cycle.h"
#include "guard_interval.h"
#include "error_handler.h"

#include <iostream>

SchedulerError scheduler_main_loop(
    ThreadSharedState* shared_state,
    ServerScheduler* scheduler,
    pcap_t* pcap_handle,
    const SchedulerConfig& config,
    std::atomic<bool>* shutdown,
    SchedulerStats* stats
) {
    // 参数验证
    if (!shared_state || !scheduler || !pcap_handle || !shutdown) {
        return SchedulerError::UNKNOWN_ERROR;
    }

    // 检查是否有节点注册
    if (scheduler->get_node_count() == 0) {
        std::cerr << "错误: 没有节点注册到调度器" << std::endl;
        return SchedulerError::NO_NODES_REGISTERED;
    }

    // 初始化呼吸周期（下行广播）
    BreathingCycle breathing_cycle;

    // Token 发射器
    TokenEmitter token_emitter(pcap_handle);

    std::cout << "调度器线程启动，节点数=" << static_cast<int>(config.node_count) 
              << ", 优先级=95" << std::endl;

    // 主调度循环
    while (!shutdown->load()) {
        // 1. 获取下一个要调度的节点
        uint8_t next_node = scheduler->get_next_scheduled_node();

        if (next_node == 0) {
            // 全员深睡，执行空闲巡逻
            scheduler->idle_patrol();
            if (stats) stats->idle_patrols++;
            
            // 长间隔空闲巡逻（100ms）
            usleep(100000);
            continue;
        }

        // 2. 计算授权时间窗口
        uint32_t time_quota_ms = scheduler->calculate_time_quota(next_node);

        // 限制在配置范围内
        time_quota_ms = std::max(config.min_window_ms,
                                 std::min(config.max_window_ms, time_quota_ms));

        // 3. 构建 TokenFrame
        TokenFrame token;
        token.magic = 0x02;
        token.target_node = next_node;
        token.time_quota_ms = time_quota_ms;
        token.seq_number = token_emitter.get_next_seq();

        // 4. 三连发 Token（击穿机制）
        bool send_success = token_emitter.send_token_triple(token);
        if (!send_success) {
            std::cerr << "Token 发射失败，节点=" << static_cast<int>(next_node) << std::endl;
            if (stats) stats->tokens_failed++;
            
            // 发射失败，等待一段时间后重试下一个节点
            usleep(10000);  // 10ms
            continue;
        }

        if (stats) stats->tokens_sent++;

        // 5. 应用保护间隔（节点切换）
        apply_guard_interval(config.guard_interval_ms);
        if (stats) stats->node_switches++;

        // 6. 更新共享状态（供发射线程使用）
        {
            std::lock_guard<std::mutex> lock(shared_state->mtx);
            shared_state->can_send = true;
            shared_state->token_expire_time_ms =
                get_monotonic_ms() + time_quota_ms;
            shared_state->cv_send.notify_one();
        }

        // 7. 等待授权窗口过期（带 shutdown 检查）
        uint64_t start_ms = get_monotonic_ms();
        while (!shutdown->load()) {
            uint64_t elapsed = get_monotonic_ms() - start_ms;
            if (elapsed >= time_quota_ms) break;
            
            // 短间隔检查（10ms）
            usleep(10000);
        }

        if (shutdown->load()) break;

        // 8. 更新节点状态
        scheduler->update_node_state(next_node);

        // 9. 呼吸周期管理（仅服务端）
        breathing_cycle.advance_phase();
        if (breathing_cycle.is_inhale_phase()) {
            // 吸气阶段：轮询所有节点的 NACK
            for (size_t node_id = 1; node_id <= scheduler->get_node_count(); node_id++) {
                scheduler->schedule_node_for_nack(static_cast<uint8_t>(node_id), 25);
            }
        }
    }

    std::cout << "调度器线程正常退出" << std::endl;
    return SchedulerError::OK;
}
```
  </action>
  <verify>
    <automated>make scheduler_worker && echo "编译通过" || echo "编译失败"</automated>
  </verify>
  <done>
    - 调度器线程正确实现
    - Token 三连发正确执行
    - 保护间隔正确应用
    - Token 发射失败有错误处理
    - 空闲巡逻正确触发
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 wfb_core.cpp 集成调度器线程</name>
  <files>src/wfb_core.cpp</files>
  <action>
在 run_server() 中启动调度器线程

```cpp
#include "scheduler_worker.h"
#include "server_scheduler.h"

int run_server(const Config& config) {
    // ... 其他线程启动 ...

    // 创建调度器实例
    ServerScheduler scheduler(config.mcs, config.bandwidth);

    // 注册节点（从配置文件或命令行）
    // 假设有 10 个节点，ID 从 1 到 10
    for (uint8_t node_id = 1; node_id <= 10; node_id++) {
        scheduler.register_node(node_id);
    }

    // 调度器配置
    SchedulerConfig sched_config = {
        .min_window_ms = config.min_window_ms,
        .max_window_ms = config.max_window_ms,
        .guard_interval_ms = config.guard_interval_ms,
        .node_count = 10
    };

    // 启动调度器线程
    SchedulerStats sched_stats;
    std::thread scheduler_thread([&]() {
        // 设置实时优先级 95（低于控制面 99，高于发射端 90）
        if (!set_thread_realtime_priority(ThreadPriority::SCHEDULER, "scheduler")) {
            std::cerr << "警告: 无法设置调度器线程实时优先级" << std::endl;
        }

        SchedulerError err = scheduler_main_loop(
            &shared_state, &scheduler, pcap_handle.get(),
            sched_config, &g_shutdown, &sched_stats);

        if (err != SchedulerError::OK) {
            std::cerr << "调度器线程异常退出: " << static_cast<int>(err) << std::endl;
        }
    });

    // ... 等待所有线程 ...

    scheduler_thread.join();

    // 输出统计信息
    std::cout << "调度器统计: Token发送=" << sched_stats.tokens_sent.load()
              << ", Token失败=" << sched_stats.tokens_failed.load()
              << ", 空闲巡逻=" << sched_stats.idle_patrols.load()
              << ", 节点切换=" << sched_stats.node_switches.load() << std::endl;

    return 0;
}
```
  </action>
  <verify>
    <automated>grep -E "scheduler_main_loop|ThreadPriority::SCHEDULER" src/wfb_core.cpp && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 调度器线程正确启动
    - 实时优先级 95 设置
    - 节点注册正确
    - 统计信息输出
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调度器 → Token 发射 | 内部控制流，可信 |
| 调度器 → 节点状态 | 内部状态管理，可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | DoS | Token 发射失败 | mitigate | 错误日志，重试下一个节点 |
| T-05-11 | Tampering | 节点状态 | mitigate | 状态机验证，非法状态拒绝 |
</threat_model>

<verification>
- 调度器线程编译通过
- Token 三连发正确执行
- 保护间隔正确应用
- Token 发射失败有错误处理
- 空闲巡逻正确触发
- SCHED_FIFO 95 优先级设置
</verification>

<success_criteria>
1. 调度器线程在 Server 模式下正确运行
2. Token 三连发正确执行
3. 保护间隔（5ms）正确应用
4. Token 发射失败有错误处理
5. SCHED_FIFO 95 优先级设置
6. 节点状态机跃迁符合设计
</success_criteria>

<output>
After completion, create `.planning/phases/05-wfb-core/05-04-SUMMARY.md`
</output>
