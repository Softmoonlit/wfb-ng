---
phase: 05
plan: 06
type: execute
wave: 6
depends_on:
  - 05-00
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - tests/test_wfb_core.cpp
  - tests/integration_test_v2.sh
  - Makefile
autonomous: true
requirements:
  - PROC-01
  - PROC-12
  - RT-08

must_haves:
  truths:
    - "所有单元测试通过（至少 8 个测试用例）"
    - "线程启动和退出正常"
    - "Token 授权和过期逻辑正确"
    - "队列反压机制正常工作"
    - "动态水位线计算符合公式"
    - "TokenFrame 序列化/反序列化正确"
    - "condition_variable 唤醒机制正常"
    - "集成测试脚本成功运行"
  artifacts:
    - path: "tests/test_wfb_core.cpp"
      provides: "单元测试套件"
      min_lines: 200
    - path: "tests/integration_test_v2.sh"
      provides: "集成测试脚本"
      contains: "wfb_core"
    - path: "Makefile"
      provides: "编译配置"
      contains: "wfb_core"
  key_links:
    - from: "tests/test_wfb_core.cpp"
      to: "src/wfb_core.cpp"
      via: "功能测试"
      pattern: "TEST.*WfbCore"
    - from: "tests/integration_test_v2.sh"
      to: "wfb_core"
      via: "端到端测试"
      pattern: "./build/wfb_core"
---

# Plan 05-06: 集成测试与验证（增强版）

<objective>
创建完整的集成测试，验证 wfb_core 单进程架构的正确性，确保所有线程协同工作、Token 分配、数据流传输、反压机制均符合设计。

**评审反馈整合**：
- 添加详细的错误处理测试
- 添加边界条件测试（Token 过期、队列满、FEC 参数）
- 添加压力测试（长时间运行、高并发）

Purpose: 验证单进程架构的正确性和稳定性
Output: 完整的测试套件，覆盖所有核心功能
</objective>

<execution_context>
@/home/kilome/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@/home/kilome/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wfb-core/05-CONTEXT.md
@.planning/phases/05-wfb-core/05-RESEARCH.md

<interfaces>
From src/threads.h:
```cpp
struct ThreadSharedState {
    std::mutex mtx;
    std::condition_variable cv_send;
    bool can_send GUARDED_BY(mtx) = false;
    uint64_t token_expire_time_ms GUARDED_BY(mtx) = 0;
    PacketQueue packet_queue;
    uint32_t dynamic_queue_limit GUARDED_BY(mtx) = 0;
    std::atomic<bool> shutdown{false};
};
```

From src/config.h:
```cpp
struct FECConfig {
    int n = 12;
    int k = 8;
    bool is_valid() const;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建核心功能单元测试</name>
  <files>tests/test_wfb_core.cpp</files>
  <behavior>
    - Test 1: 线程启动和退出正常
    - Test 2: Token 授权和过期逻辑正确
    - Test 3: 队列反压机制正常
    - Test 4: 动态水位线计算正确
    - Test 5: TokenFrame 序列化/反序列化正确
    - Test 6: condition_variable 唤醒正常
    - Test 7: FEC 参数配置正确
    - Test 8: 错误处理正确
  </behavior>
  <action>
创建完整的单元测试套件

```cpp
#include <gtest/gtest.h>
#include <gmock/gmock.h>
#include <thread>
#include <chrono>
#include <atomic>
#include <vector>

#include "threads.h"
#include "packet_queue.h"
#include "watermark.h"
#include "mac_token.h"
#include "config.h"
#include "error_handler.h"

// === 测试 1: 线程启动和退出 ===
TEST(WfbCoreTest, ThreadLifecycle) {
    ThreadSharedState state;
    std::atomic<bool> shutdown{false};

    std::thread control_thread([&]() {
        int iterations = 0;
        while (!shutdown.load() && iterations < 10) {
            std::unique_lock<std::mutex> lock(state.mtx);
            state.can_send = true;
            state.token_expire_time_ms = get_monotonic_ms() + 50;
            state.cv_send.notify_one();
            lock.unlock();
            std::this_thread::sleep_for(std::chrono::milliseconds(10));
            iterations++;
        }
    });

    // 运行 100ms
    std::this_thread::sleep_for(std::chrono::milliseconds(100));

    // 触发退出
    shutdown.store(true);
    control_thread.join();

    EXPECT_TRUE(true);  // 线程正常退出
}

// === 测试 2: Token 授权和过期 ===
TEST(WfbCoreTest, TokenGating) {
    ThreadSharedState state;

    // 模拟 Token 授权
    {
        std::lock_guard<std::mutex> lock(state.mtx);
        state.can_send = true;
        state.token_expire_time_ms = get_monotonic_ms() + 50;  // 50ms
    }

    // 立即检查：应允许发射
    {
        std::lock_guard<std::mutex> lock(state.mtx);
        EXPECT_TRUE(state.can_send);
        EXPECT_GT(state.token_expire_time_ms, get_monotonic_ms());
    }

    // 等待 Token 过期
    std::this_thread::sleep_for(std::chrono::milliseconds(60));

    // 检查：Token 应已过期
    {
        std::lock_guard<std::mutex> lock(state.mtx);
        EXPECT_GT(get_monotonic_ms(), state.token_expire_time_ms);
    }
}

// === 测试 3: 环形队列反压 ===
TEST(WfbCoreTest, QueueBackpressure) {
    PacketQueue queue;
    queue.set_capacity(100);

    // 填满队列
    PacketQueue::Packet pkt;
    memset(pkt.data, 0xAA, sizeof(pkt.data));
    pkt.len = 100;

    int push_count = 0;
    while (queue.push(pkt)) {
        push_count++;
        if (push_count > 150) break;  // 安全限制
    }

    // 队列满时应拒绝 push
    EXPECT_GE(push_count, 100);
    EXPECT_LT(push_count, 150);

    // 检查队列大小
    EXPECT_EQ(queue.size(), static_cast<size_t>(queue.capacity()));

    // 清空队列
    int pop_count = 0;
    PacketQueue::Packet out;
    while (queue.try_pop(out)) {
        pop_count++;
    }

    EXPECT_EQ(pop_count, push_count);
    EXPECT_EQ(queue.size(), 0u);
}

// === 测试 4: 动态水位线计算 ===
TEST(WfbCoreTest, DynamicWatermark) {
    // MCS 0, 20MHz, 50ms 窗口
    uint32_t limit1 = calculate_dynamic_limit(50, 6000000, 1450);
    EXPECT_GT(limit1, 0u);
    EXPECT_LT(limit1, 1000u);  // 应在合理范围内

    // MCS 6, 20MHz, 50ms 窗口（更高速率）
    uint32_t limit2 = calculate_dynamic_limit(50, 54000000, 1450);
    EXPECT_GT(limit2, limit1);  // 应比低速时更大

    // 验证公式：limit = ceil((R_phy × duration × 1.5) / (8 × MTU × 1000))
    double expected = std::ceil((6000000.0 * 50 * 1.5) / (8 * 1450 * 1000));
    EXPECT_NEAR(limit1, expected, 1.0);

    // 边界测试：极端参数
    uint32_t limit_zero = calculate_dynamic_limit(0, 6000000, 1450);
    EXPECT_EQ(limit_zero, 0u);

    uint32_t limit_max = calculate_dynamic_limit(1000, 54000000, 1450);
    EXPECT_GT(limit_max, 0u);
}

// === 测试 5: TokenFrame 序列化和反序列化 ===
TEST(WfbCoreTest, TokenFrameSerialization) {
    TokenFrame original;
    original.magic = 0x02;
    original.target_node = 5;
    original.time_quota_ms = 300;
    original.seq_number = 12345;

    // 序列化
    uint8_t buffer[TokenFrame::kSerializedSize];
    ASSERT_TRUE(original.serialize(buffer, sizeof(buffer)));

    // 反序列化
    TokenFrame decoded;
    ASSERT_TRUE(decoded.deserialize(buffer, sizeof(buffer)));

    // 验证字段
    EXPECT_EQ(decoded.magic, original.magic);
    EXPECT_EQ(decoded.target_node, original.target_node);
    EXPECT_EQ(decoded.time_quota_ms, original.time_quota_ms);
    EXPECT_EQ(decoded.seq_number, original.seq_number);
}

// === 测试 6: condition_variable 唤醒 ===
TEST(WfbCoreTest, ConditionVariableWakeup) {
    ThreadSharedState state;
    std::atomic<bool> woke_up{false};

    // 等待线程
    std::thread waiter([&]() {
        std::unique_lock<std::mutex> lock(state.mtx);
        state.cv_send.wait(lock, [&]() {
            return state.can_send;
        });
        woke_up.store(true);
    });

    // 等待 10ms
    std::this_thread::sleep_for(std::chrono::milliseconds(10));

    // 唤醒线程
    {
        std::lock_guard<std::mutex> lock(state.mtx);
        state.can_send = true;
        state.cv_send.notify_one();
    }

    // 等待线程响应
    waiter.join();

    EXPECT_TRUE(woke_up.load());
}

// === 测试 7: FEC 参数配置 ===
TEST(WfbCoreTest, FECConfigValidation) {
    // 默认配置
    FECConfig config1;
    EXPECT_TRUE(config1.is_valid());
    EXPECT_EQ(config1.n, 12);
    EXPECT_EQ(config1.k, 8);
    EXPECT_EQ(config1.redundancy(), 4);

    // 自定义配置
    FECConfig config2{16, 10};
    EXPECT_TRUE(config2.is_valid());
    EXPECT_EQ(config2.redundancy(), 6);

    // 无效配置
    FECConfig config3{8, 12};  // k > n
    EXPECT_FALSE(config3.is_valid());

    FECConfig config4{0, 8};  // n = 0
    EXPECT_FALSE(config4.is_valid());

    FECConfig config5{300, 10};  // n > 255
    EXPECT_FALSE(config5.is_valid());
}

// === 测试 8: 错误处理 ===
TEST(WfbCoreTest, ErrorHandling) {
    ErrorContext ctx;
    ctx.function = "test_function";
    ctx.file = "test.cpp";
    ctx.line = 100;
    ctx.errno_value = EINVAL;
    ctx.message = "测试错误";
    ctx.severity = ErrorSeverity::ERROR;

    // 错误日志函数不应崩溃
    testing::internal::CaptureStderr();
    log_error(ctx);
    std::string output = testing::internal::GetCapturedStderr();
    
    EXPECT_FALSE(output.empty());
    EXPECT_NE(output.find("test_function"), std::string::npos);
}

// === 测试 9: 并发安全性 ===
TEST(WfbCoreTest, ConcurrentAccess) {
    ThreadSharedState state;
    std::atomic<bool> shutdown{false};
    std::atomic<int> operations{0};

    // 多线程并发访问
    std::vector<std::thread> threads;
    for (int i = 0; i < 4; i++) {
        threads.emplace_back([&]() {
            while (!shutdown.load()) {
                {
                    std::lock_guard<std::mutex> lock(state.mtx);
                    state.can_send = !state.can_send;
                    state.token_expire_time_ms = get_monotonic_ms() + 50;
                }
                operations++;
                std::this_thread::sleep_for(std::chrono::milliseconds(1));
            }
        });
    }

    // 运行 100ms
    std::this_thread::sleep_for(std::chrono::milliseconds(100));
    shutdown.store(true);

    // 等待所有线程
    for (auto& t : threads) {
        t.join();
    }

    EXPECT_GT(operations.load(), 0);
}

// === 测试 10: 内存泄漏检测（简化） ===
TEST(WfbCoreTest, NoMemoryLeak) {
    // 创建和销毁多次
    for (int i = 0; i < 100; i++) {
        PacketQueue queue;
        queue.set_capacity(100);
        
        PacketQueue::Packet pkt;
        pkt.len = 100;
        memset(pkt.data, 0, sizeof(pkt.data));
        
        for (int j = 0; j < 50; j++) {
            queue.push(pkt);
        }
        
        PacketQueue::Packet out;
        while (queue.try_pop(out)) {
            // 空操作
        }
    }
    
    EXPECT_TRUE(true);  // 如果没有崩溃，则通过
}

int main(int argc, char** argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
```
  </action>
  <verify>
    <automated>grep -E "TEST.*WfbCore|EXPECT_" tests/test_wfb_core.cpp | wc -l | xargs -I{} test {} -ge 20 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 10 个测试用例覆盖核心功能
    - Token 授权和过期测试
    - 队列反压测试
    - 动态水位线计算测试
    - TokenFrame 序列化测试
    - condition_variable 测试
    - FEC 配置验证测试
    - 错误处理测试
    - 并发安全测试
    - 内存泄漏检测
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建集成测试脚本</name>
  <files>tests/integration_test_v2.sh</files>
  <action>
创建端到端集成测试脚本

```bash
#!/bin/bash

# Phase 5 集成测试脚本
# 测试单进程架构的端到端功能

set -e

echo "=== Phase 5 集成测试 ==="
echo "测试日期: $(date)"

# 测试计数器
TOTAL_TESTS=0
PASSED_TESTS=0
FAILED_TESTS=0

# 测试函数
run_test() {
    local test_name="$1"
    local test_cmd="$2"
    
    TOTAL_TESTS=$((TOTAL_TESTS + 1))
    echo ""
    echo "[$TOTAL_TESTS] $test_name"
    echo "命令: $test_cmd"
    
    if eval "$test_cmd"; then
        PASSED_TESTS=$((PASSED_TESTS + 1))
        echo "✓ 通过"
    else
        FAILED_TESTS=$((FAILED_TESTS + 1))
        echo "✗ 失败"
    fi
}

# === 1. 编译测试 ===
echo ""
echo "=== 1. 编译测试 ==="

run_test "清理编译产物" "make clean"

run_test "编译 wfb_core" "make wfb_core"

run_test "编译测试程序" "make test_wfb_core"

# === 2. 单元测试 ===
echo ""
echo "=== 2. 单元测试 ==="

run_test "运行单元测试" "./tests/test_wfb_core"

# === 3. 命令行参数测试 ===
echo ""
echo "=== 3. 命令行参数测试 ==="

run_test "显示帮助信息" "./build/wfb_core --help | grep -q '用法'"

run_test "验证服务端参数" "./build/wfb_core --mode server -i wlan0 -c 6 -m 0 --help | grep -q 'server'"

run_test "验证客户端参数" "./build/wfb_core --mode client -i wlan0 -c 6 -m 0 --node-id 1 --help | grep -q 'client'"

run_test "验证 FEC 参数" "./build/wfb_core --mode server -i wlan0 --fec-n 16 --fec-k 10 --help | grep -q 'fec'"

# === 4. 权限检查 ===
echo ""
echo "=== 4. 权限检查 ==="

if [ "$EUID" -ne 0 ]; then
    echo "警告: 未以 Root 权限运行，跳过实时调度和进程启动测试"
    echo "使用 sudo 运行此脚本以执行完整测试"
else
    echo "✓ Root 权限确认"
    
    # === 5. 进程启动测试 ===
    echo ""
    echo "=== 5. 进程启动测试 ==="
    
    # 检查是否有可用的 WiFi 接口
    WIFI_IFACE=$(ip link show | grep -oP 'wl\w+' | head -1)
    
    if [ -z "$WIFI_IFACE" ]; then
        echo "警告: 未检测到 WiFi 接口，使用虚拟接口测试"
        WIFI_IFACE="lo"  # 使用回环接口进行基本测试
    fi
    
    echo "使用接口: $WIFI_IFACE"
    
    # 启动服务端（后台，2 秒后终止）
    echo "[5.1] 启动服务端..."
    timeout 2 ./build/wfb_core --mode server -i "$WIFI_IFACE" -c 6 -m 0 --tun wfb0 2>&1 || true
    echo "✓ 服务端启动测试完成"
    
    # 启动客户端（后台，2 秒后终止）
    echo "[5.2] 启动客户端..."
    timeout 2 ./build/wfb_core --mode client -i "$WIFI_IFACE" -c 6 -m 0 --tun wfb1 --node-id 1 2>&1 || true
    echo "✓ 客户端启动测试完成"
    
    # === 6. 启动脚本测试 ===
    echo ""
    echo "=== 6. 启动脚本测试 ==="
    
    if [ -f "./scripts/server_start.sh" ]; then
        echo "[6.1] 检查服务端启动脚本..."
        if grep -q "wfb_core --mode server" "./scripts/server_start.sh"; then
            echo "✓ 服务端启动脚本正确调用 wfb_core"
        else
            echo "✗ 服务端启动脚本未调用 wfb_core"
            FAILED_TESTS=$((FAILED_TESTS + 1))
        fi
    fi
    
    if [ -f "./scripts/client_start.sh" ]; then
        echo "[6.2] 检查客户端启动脚本..."
        if grep -q "wfb_core --mode client" "./scripts/client_start.sh"; then
            echo "✓ 客户端启动脚本正确调用 wfb_core"
        else
            echo "✗ 客户端启动脚本未调用 wfb_core"
            FAILED_TESTS=$((FAILED_TESTS + 1))
        fi
    fi
fi

# === 7. 文档检查 ===
echo ""
echo "=== 7. 文档检查 ==="

run_test "部署文档存在" "test -f docs/deployment.md"

run_test "部署文档包含单进程说明" "grep -q '单进程模式' docs/deployment.md"

# === 测试总结 ===
echo ""
echo "=== 测试总结 ==="
echo "总测试数: $TOTAL_TESTS"
echo "通过: $PASSED_TESTS"
echo "失败: $FAILED_TESTS"
echo ""

if [ $FAILED_TESTS -eq 0 ]; then
    echo "✓ 所有测试通过！"
    exit 0
else
    echo "✗ 有 $FAILED_TESTS 个测试失败"
    exit 1
fi
```
  </action>
  <verify>
    <automated>grep -E "run_test|PASSED_TESTS|FAILED_TESTS" tests/integration_test_v2.sh | wc -l | xargs -I{} test {} -ge 10 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 集成测试脚本完整
    - 编译测试
    - 单元测试执行
    - 命令行参数测试
    - 权限检查
    - 进程启动测试
    - 启动脚本检查
    - 文档检查
    - 测试总结报告
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 Makefile</name>
  <files>Makefile</files>
  <action>
添加 wfb_core 编译目标和测试目标

```makefile
# === wfb_core 单进程编译 ===

# 源文件
WFB_CORE_SRC = src/wfb_core.cpp \
               src/config.cpp \
               src/rx_demux.cpp \
               src/tx_worker.cpp \
               src/tun_reader.cpp \
               src/scheduler_worker.cpp \
               src/server_scheduler.cpp \
               src/aq_sq_manager.cpp \
               src/breathing_cycle.cpp \
               src/watermark.cpp \
               src/radiotap_template.cpp \
               src/token_emitter.cpp \
               src/zfex.c

# 目标文件
WFB_CORE_OBJ = $(patsubst %.cpp,%.o,$(filter %.cpp,$(WFB_CORE_SRC)))
WFB_CORE_OBJ := $(patsubst %.c,%.o,$(WFB_CORE_OBJ))

# 编译标志
CXXFLAGS += -std=c++11 -Wall -Wextra -O2 -g
CFLAGS += -Wall -O2 -g
LDFLAGS += -lpcap -lpthread -lrt

# 头文件路径
INCLUDES = -I./src -I./include

# wfb_core 目标
wfb_core: $(WFB_CORE_OBJ)
	$(CXX) $(CXXFLAGS) -o $@ $^ $(LDFLAGS)
	@echo "✓ wfb_core 编译成功"

# 通用编译规则
%.o: %.cpp
	$(CXX) $(CXXFLAGS) $(INCLUDES) -c $< -o $@

%.o: %.c
	$(CC) $(CFLAGS) $(INCLUDES) -c $< -o $@

# === 测试编译 ===

# 测试源文件
TEST_SRC = tests/test_wfb_core.cpp

# 测试目标
test_wfb_core: $(TEST_SRC) $(filter-out src/wfb_core.o,$(WFB_CORE_OBJ))
	$(CXX) $(CXXFLAGS) $(INCLUDES) -o $@ $^ $(LDFLAGS) -lgtest -lgtest_main -lgmock
	@echo "✓ test_wfb_core 编译成功"

# 运行测试
test: test_wfb_core
	./test_wfb_core

# 运行集成测试
integration_test: integration_test_v2.sh
	chmod +x tests/integration_test_v2.sh
	./tests/integration_test_v2.sh

# 清理
clean:
	rm -f $(WFB_CORE_OBJ) wfb_core test_wfb_core
	@echo "✓ 清理完成"

# 依赖关系
src/wfb_core.o: src/wfb_core.cpp src/config.h src/threads.h src/rx_demux.h src/tx_worker.h src/tun_reader.h src/scheduler_worker.h
src/rx_demux.o: src/rx_demux.cpp src/rx_demux.h src/threads.h src/resource_guard.h
src/tx_worker.o: src/tx_worker.cpp src/tx_worker.h src/threads.h src/resource_guard.h
src/tun_reader.o: src/tun_reader.cpp src/tun_reader.h src/threads.h src/resource_guard.h
src/scheduler_worker.o: src/scheduler_worker.cpp src/scheduler_worker.h src/server_scheduler.h

.PHONY: wfb_core test_wfb_core test integration_test clean
```
  </action>
  <verify>
    <automated>grep -E "wfb_core:|test_wfb_core:|WFB_CORE_SRC" Makefile && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - wfb_core 编译目标
    - test_wfb_core 测试目标
    - 依赖关系正确
    - 清理目标完整
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 被测代码 | 内部测试，可信 |
| 集成测试 → 系统资源 | 测试环境，需隔离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | DoS | 集成测试 | accept | 测试环境，可控 |
</threat_model>

<verification>
- 所有单元测试通过
- 集成测试脚本成功运行
- wfb_core 编译正确
- 测试覆盖核心功能
</verification>

<success_criteria>
1. 所有单元测试通过（至少 10 个测试用例）
2. 线程启动和退出正常
3. Token 授权和过期逻辑正确
4. 队列反压机制正常工作
5. 动态水位线计算符合公式
6. TokenFrame 序列化/反序列化正确
7. condition_variable 唤醒机制正常
8. FEC 参数配置正确
9. 错误处理正确
10. 集成测试脚本成功运行
</success_criteria>

<output>
After completion, create `.planning/phases/05-wfb-core/05-06-SUMMARY.md`
</output>
