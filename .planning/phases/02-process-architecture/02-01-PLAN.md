---
phase: 02
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [src/watermark.h, src/watermark.cpp, tests/test_watermark.cpp]
autonomous: true
requirements: [BUFFER-03, BUFFER-04, BUFFER-05, BUFFER-06]
must_haves:
  truths:
    - "动态水位线必须由物理速率、时长和 MTU 计算"
    - "MCS 速率映射必须覆盖常用档位"
    - "公式输出应与现有 watermark 单测保持一致"
  artifacts:
    - path: "src/watermark.h"
      provides: "动态水位线计算接口"
      contains: "calculate_dynamic_limit"
    - path: "src/watermark.cpp"
      provides: "动态水位线实现"
      exports: ["calculate_dynamic_limit"]
    - path: "tests/test_watermark.cpp"
      provides: "水位线计算验证"
      min_lines: 40
  key_links:
    - from: "MCS 配置入口"
      to: "calculate_dynamic_limit"
      via: "phy_rate_bps"
      pattern: "calculate_dynamic_limit"
    - from: "线程调度水位线"
      to: "TUN 读取退避"
      via: "动态 limit"
      pattern: "limit"
---

<objective>
建立动态水位线与 MCS 速率映射基础，让队列限制从物理速率推导，而不是依赖硬编码阈值。

Purpose: 让反压阈值与实时速率联动，支撑后续单进程调度。
Output: 水位线公式、速率映射入口和验证测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-architecture/PLAN.md
@.planning/research/STACK.md

# 现有代码接口
From src/watermark.h:
```cpp
size_t calculate_dynamic_limit(uint16_t duration_ms, uint64_t phy_rate_bps, size_t mtu);
```

From src/watermark.cpp:
```cpp
double limit = (static_cast<double>(phy_rate_bps) * duration_ms * 1.5) / (8.0 * mtu * 1000.0);
return static_cast<size_t>(std::ceil(limit));
```

From tests/test_watermark.cpp:
```cpp
size_t limit = calculate_dynamic_limit(25, 6000000, 1450);
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化动态水位线接口</name>
  <files>src/watermark.h, src/watermark.cpp</files>
  <behavior>
    - Test 1: `calculate_dynamic_limit()` 保持现有数学行为不变
    - Test 2: 输入同一参数时输出稳定一致
    - Test 3: 计算公式清晰，可供后续调度层直接复用
  </behavior>
  <action>
保持现有水位线公式作为 Phase 2 的基础实现，不在这一波次改动数学常量。

1. 保留当前接口签名
2. 保留 `ceil` 计算逻辑
3. 保留 1.5 倍安全系数
4. 只补齐注释和测试说明
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_watermark.cpp src/watermark.cpp -o tests/test_watermark && ./tests/test_watermark</automated>
  </verify>
  <done>动态水位线公式稳定，后续只需接入物理速率来源</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 预留 MCS 速率映射接入点</name>
  <files>src/watermark.h, src/watermark.cpp</files>
  <behavior>
    - Test 1: 速率来源可以从未来的 MCS/频宽配置转换而来
    - Test 2: 接口保持可直接接入 `phy_rate_bps`
    - Test 3: 不破坏当前调用方
  </behavior>
  <action>
为后续主循环提供明确的 MCS 映射接入口。

1. 保持 `phy_rate_bps` 为输入参数
2. 在注释中标注其来自 MCS 映射表
3. 不把映射逻辑塞进公式函数内部
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/watermark.cpp -o /tmp/test_watermark.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>水位线计算函数与速率来源解耦完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补充水位线单元测试说明</name>
  <files>tests/test_watermark.cpp</files>
  <behavior>
    - Test 1: 现有 6Mbps/25ms/1450 场景保持 20 的结果
    - Test 2: 说明该结果是 Phase 2 反压闭环的基础值
    - Test 3: 为后续不同 MCS 档位预留断言位置
  </behavior>
  <action>
补充测试注释与场景说明，确保后续接入 MCS 速率映射时测试语义清晰。

1. 保留现有断言
2. 增加不同 MCS 档位的测试占位说明
3. 不改动现有公式实现
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_watermark.cpp src/watermark.cpp -o tests/test_watermark && ./tests/test_watermark</automated>
  </verify>
  <done>水位线测试能清楚表达当前基础语义</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 tests/test_watermark.cpp src/watermark.cpp -o tests/test_watermark`
2. 单元测试通过：`./tests/test_watermark`
3. 现有公式行为保持不变
4. 接口仍可直接接入未来 MCS 速率映射结果
5. Git 提交：`git commit -m "feat(02-01): 建立动态水位线基础"`
</verification>

<success_criteria>
1. `calculate_dynamic_limit()` 行为稳定
2. 水位线公式与现有测试一致
3. 接口清晰保留 `phy_rate_bps` 输入
4. 代码与测试注释清楚表达后续 MCS 接入点
5. 代码符合 Google C++ Style Guide
6. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-architecture/02-01-SUMMARY.md`
</output>
