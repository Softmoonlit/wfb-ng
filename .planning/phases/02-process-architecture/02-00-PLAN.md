---
phase: 02
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [src/packet_queue.h, tests/test_packet_queue.cpp]
autonomous: true
requirements: [BUFFER-01, BUFFER-02]
must_haves:
  truths:
    - "ThreadSafeQueue 使用静态分配的环形缓冲区"
    - "队列满时直接丢弃新包，不阻塞等待"
    - "基础队列语义可被后续线程架构复用"
  artifacts:
    - path: "src/packet_queue.h"
      provides: "线程安全环形队列"
      contains: "ThreadSafeQueue<T>"
    - path: "tests/test_packet_queue.cpp"
      provides: "队列语义验证"
      min_lines: 40
  key_links:
    - from: "TUN 读取线程"
      to: "ThreadSafeQueue<T>::push"
      via: "入队"
      pattern: "push("
    - from: "发射线程"
      to: "ThreadSafeQueue<T>::pop"
      via: "出队"
      pattern: "pop("
---

<objective>
完善线程安全环形队列的语义，确保容量固定、满时丢弃新包、空时阻塞由调用方处理，为后续反压链路提供稳定基础。

Purpose: 让队列成为单进程架构中的稳定缓冲层，避免阻塞式积压。
Output: 队列实现强化和补充测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-architecture/PLAN.md
@.planning/research/STACK.md

# 现有代码接口
From src/packet_queue.h:
```cpp
template<typename T>
class ThreadSafeQueue {
public:
    ThreadSafeQueue(size_t capacity);
    void push(const T& item);
    T pop();
    size_t size();
    bool empty();
};
```

From tests/test_packet_queue.cpp:
```cpp
struct DummyPacket { int id; };
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定队列容量语义</name>
  <files>src/packet_queue.h</files>
  <behavior>
    - Test 1: 构造函数接收容量后，队列内部缓冲区按容量静态分配
    - Test 2: `size()` 和 `empty()` 在初始状态返回正确结果
    - Test 3: 队列对象可以安全容纳后续线程入口调用
  </behavior>
  <action>
确认 `ThreadSafeQueue` 的容量语义保持固定，不引入动态扩容或阻塞式等待。

1. 保留现有构造函数签名
2. 保留 `std::vector<T>` 的预分配策略
3. 保持 `push()` 的非阻塞丢弃语义
4. 不引入新的外部依赖
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/packet_queue.h -o /tmp/test_packet_queue.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>队列容量语义稳定，后续线程可以直接复用</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 完善满队列丢弃行为</name>
  <files>src/packet_queue.h, tests/test_packet_queue.cpp</files>
  <behavior>
    - Test 1: 队列达到容量后，新的 `push()` 调用不增加 size
    - Test 2: 队列满时不会抛异常或阻塞
    - Test 3: `pop()` 仍然按 FIFO 顺序返回已入队元素
  </behavior>
  <action>
在当前实现上保持满队列直接丢弃的行为，并补充测试覆盖这个语义。

1. 保留 `if (count == max_capacity) return;`
2. 在测试中覆盖满队列丢弃场景
3. 保持 FIFO 出队顺序不变
4. 不把丢弃改成等待或抛错
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_packet_queue.cpp -o tests/test_packet_queue && ./tests/test_packet_queue</automated>
  </verify>
  <done>满队列丢弃行为明确且有测试覆盖</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补充简单并发测试</name>
  <files>tests/test_packet_queue.cpp</files>
  <behavior>
    - Test 1: 多次连续 push/pop 不破坏队列状态
    - Test 2: size() 与 empty() 在交替操作后保持一致
    - Test 3: 队列语义适合后续线程调用场景
  </behavior>
  <action>
补充最小并发语义测试，验证当前队列适合后续线程架构接入。

1. 添加连续入队/出队测试
2. 添加 size/empty 交替断言
3. 保持测试简洁，不引入线程竞争测试框架
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_packet_queue.cpp -o tests/test_packet_queue && ./tests/test_packet_queue</automated>
  </verify>
  <done>队列基础行为测试完整</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 tests/test_packet_queue.cpp -o tests/test_packet_queue`
2. 单元测试通过：`./tests/test_packet_queue`
3. 队列满时保持丢弃语义，不阻塞不抛错
4. 代码符合现有队列接口，不引入额外复杂度
5. Git 提交：`git commit -m "feat(02-00): 完善线程安全队列语义"`
</verification>

<success_criteria>
1. `ThreadSafeQueue` 继续保持固定容量环形缓冲区语义
2. 队列满时直接丢弃新包
3. 测试覆盖初始、满载和 FIFO 场景
4. 实现保持轻量，便于后续主循环复用
5. 代码符合 Google C++ Style Guide
6. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-architecture/02-00-SUMMARY.md`
</output>
