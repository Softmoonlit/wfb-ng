---
phase: 02
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [src/threads.h, src/wfb_core.cpp, tests/test_threads.cpp]
autonomous: true
requirements: [RT-01, RT-02, RT-03, RT-04, RT-05, RT-06, RT-07, RT-08]
must_haves:
  truths:
    - "关键线程必须使用 SCHED_FIFO 提权"
    - "控制面和发射端优先级必须分别是 99 和 90"
    - "TUN 读取线程必须保持普通优先级"
    - "5ms 保护间隔必须通过 timerfd + 短自旋实现"
  artifacts:
    - path: "src/threads.h"
      provides: "实时调度辅助声明"
      contains: "set_thread_realtime_priority"
    - path: "src/wfb_core.cpp"
      provides: "线程提权与定时逻辑"
      contains: ["pthread_setschedparam", "timerfd_create", "clock_gettime"]
    - path: "tests/test_threads.cpp"
      provides: "实时调度和定时测试"
      min_lines: 40
  key_links:
    - from: "启动路径"
      to: "set_thread_realtime_priority"
      via: "线程初始化"
      pattern: "pthread_setschedparam"
    - from: "保护间隔"
      to: "timerfd_create"
      via: "高精度定时"
      pattern: "timerfd_create"
---

<objective>
实现实时调度提权、高精度定时和权限检测，让控制面、发射端和 TUN 读取线程形成明确的优先级边界。

Purpose: 降低调度毛刺，确保关键线程在 Linux 上以实时优先级运行。
Output: 提权函数、Root 检测、高精度休眠和测试。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-architecture/PLAN.md
@.planning/research/PITFALLS.md

# 现有代码接口
From src/threads.h:
```cpp
void set_thread_realtime_priority(int priority);
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现线程提权辅助函数</name>
  <files>src/threads.h, src/wfb_core.cpp</files>
  <behavior>
    - Test 1: 可通过 `pthread_setschedparam` 设置 `SCHED_FIFO`
    - Test 2: 失败时给出明确告警
    - Test 3: 接口可被不同线程复用
  </behavior>
  <action>
实现统一的实时优先级设置入口。

1. 声明 `set_thread_realtime_priority()`
2. 使用 `pthread_setschedparam` 设置 `SCHED_FIFO`
3. 保留失败告警信息
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>实时优先级接口就绪</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固定线程优先级边界</name>
  <files>src/wfb_core.cpp, src/threads.h</files>
  <behavior>
    - Test 1: 控制面线程使用 99
    - Test 2: 发射端线程使用 90
    - Test 3: TUN 读取线程保持普通优先级
  </behavior>
  <action>
把线程优先级边界固定到阶段目标中。

1. 控制面设置为 99
2. 发射端设置为 90
3. TUN 读取线程不提权
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>线程优先级边界明确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现高精度保护间隔定时</name>
  <files>src/wfb_core.cpp, tests/test_threads.cpp</files>
  <behavior>
    - Test 1: 可调用 `timerfd_create()` 建立高精度定时器
    - Test 2: `clock_gettime()` 可提供纳秒级时间基准
    - Test 3: 5ms 保护间隔由 timerfd + 极短自旋实现
  </behavior>
  <action>
实现高精度休眠辅助逻辑，支撑节点切换保护间隔。

1. 使用 `timerfd_create()`
2. 使用 `clock_gettime()`
3. 保留短自旋窗口，不回退到粗粒度 sleep
  </action>
  <verify>
    <automated>g++ -std=c++11 tests/test_threads.cpp src/wfb_core.cpp -o tests/test_threads -lrt && ./tests/test_threads</automated>
  </verify>
  <done>高精度保护间隔可验证</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 tests/test_threads.cpp src/wfb_core.cpp -o tests/test_threads -lrt`
2. 控制面/发射端/TUN 读取线程优先级符合要求
3. 高精度休眠通过 timerfd + 短自旋实现
4. Root 权限不足时有明确告警
5. Git 提交：`git commit -m "feat(02-04): 完成实时调度与高精度定时"`
</verification>

<success_criteria>
1. `set_thread_realtime_priority()` 可复用并稳定工作
2. 控制面优先级 99，发射端 90，TUN 线程保留普通优先级
3. 启动时可检测 Root 权限
4. `timerfd_create()` 与 `clock_gettime()` 均被接入
5. 5ms 保护间隔精度满足阶段要求
6. 代码符合 Google C++ Style Guide
7. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-architecture/02-04-SUMMARY.md`
</output>
