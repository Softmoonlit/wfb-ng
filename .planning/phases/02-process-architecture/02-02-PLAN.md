---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: [02-00, 02-01]
files_modified: [src/wfb_core.cpp, src/threads.h, src/packet_queue.h, src/watermark.h, src/watermark.cpp, src/wfb_tun.c]
autonomous: true
requirements: [BUFFER-07, BUFFER-08, BUFFER-09, BUFFER-10, PROC-01, PROC-02, PROC-03, PROC-04, PROC-06, PROC-08, PROC-09, PROC-12]
must_haves:
  truths:
    - "wfb_tun 与 wfb_tx 必须合并到单进程主循环"
    - "TUN 读取达到动态水位线后必须停止继续读包"
    - "控制面接收线程必须使用 poll() 驱动唤醒"
    - "TUN 初始化时 txqueuelen=100 的语义必须保留"
  artifacts:
    - path: "src/wfb_core.cpp"
      provides: "单进程主循环"
      contains: "main"
    - path: "src/threads.h"
      provides: "线程角色与共享状态定义"
      contains: ["tun_reader_thread", "rx_demux_thread"]
    - path: "src/packet_queue.h"
      provides: "线程安全队列基础"
      contains: "ThreadSafeQueue"
    - path: "src/watermark.cpp"
      provides: "动态水位线实现"
      contains: "calculate_dynamic_limit"
  key_links:
    - from: "TUN 读取线程"
      to: "dynamic_queue_limit"
      via: "背压判断"
      pattern: "dynamic_queue_limit"
    - from: "control_fd"
      to: "poll()"
      via: "控制面唤醒"
      pattern: "poll("
---

<objective>
把独立的 wfb_tun 和 wfb_tx 逻辑合并为单进程主循环，建立控制/数据面分流和反压闭环，避免 UDP 通信环路丢包。

Purpose: 形成 Phase 2 的系统骨架，让队列、水位线和控制面唤醒串成闭环。
Output: 单进程入口、线程定义、主循环接线与反压链路。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-architecture/PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

# 现有代码接口
From src/wfb_tun.c:
```c
int main (int argc, char *argv[]);
```

From src/packet_queue.h:
```cpp
template<typename T>
class ThreadSafeQueue;
```

From src/watermark.h:
```cpp
size_t calculate_dynamic_limit(uint16_t duration_ms, uint64_t phy_rate_bps, size_t mtu);
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义单进程入口与线程角色</name>
  <files>src/wfb_core.cpp, src/threads.h</files>
  <behavior>
    - Test 1: 新入口可被编译器识别为独立 main
    - Test 2: 线程角色和共享状态可被后续实现引用
    - Test 3: 入口结构清楚分离控制面和数据面职责
  </behavior>
  <action>
建立 `wfb_core` 作为单进程入口，并定义线程角色、共享状态和主循环骨架。

1. 新增 `src/wfb_core.cpp`
2. 新增 `src/threads.h`
3. 只放入口、线程声明和共享状态，不把业务逻辑塞满入口
4. 保留旧程序作为对照，不破坏现有入口
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>单进程入口和线程分工清晰可见</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 连接 TUN 读取、队列和动态水位线</name>
  <files>src/wfb_core.cpp, src/packet_queue.h, src/watermark.cpp</files>
  <behavior>
    - Test 1: TUN 读取线程可以按水位线判断是否继续读包
    - Test 2: 达到 limit 后停止读取并让系统反压
    - Test 3: 队列与水位线之间的数据流清晰
  </behavior>
  <action>
把 TUN 读取、队列入队和动态水位线组合成反压闭环。

1. 读取线程在队列达到 limit 后停止继续调用 read()
2. 保持 `ThreadSafeQueue` 作为中间缓冲
3. 使用 `calculate_dynamic_limit()` 作为 limit 来源
4. 保留 `txqueuelen=100` 的系统语义
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>反压闭环在主循环层面接通</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 接入控制面 poll() 唤醒路径</name>
  <files>src/wfb_core.cpp, src/wfb_tun.c</files>
  <behavior>
    - Test 1: 控制面事件可通过 poll() 唤醒
    - Test 2: 不再依赖 usleep() 轮询
    - Test 3: 控制面路径与数据面路径职责分离
  </behavior>
  <action>
把控制面接收逻辑抽象成单独线程路径，使用 `poll()` 实现唤醒。

1. 从 `wfb_tun.c` 的事件循环中抽出控制面职责
2. 保留 `poll()` 驱动事件模型
3. 不把控制面逻辑和数据面逻辑混在同一个阻塞循环里
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>控制面分流与唤醒路径建立</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 固化进程合并边界</name>
  <files>src/wfb_core.cpp, src/wfb_tun.c</files>
  <behavior>
    - Test 1: 新主循环不再通过 UDP 环路串联本地进程
    - Test 2: 旧入口仍可作为参考保留
    - Test 3: 合并边界清楚，不引入额外跨进程依赖
  </behavior>
  <action>
明确单进程边界，防止后续实现重新回到双进程 UDP 环路。

1. 在新主循环中直接串联读包与发射路径
2. 不引入本地 UDP 中转
3. 保持旧 `wfb_tun.c` 作为阶段对照，不把新逻辑塞回旧架构
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>单进程边界明确，架构不回退</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 -c src/wfb_core.cpp`
2. 控制/数据面职责分离清晰
3. TUN 读取与队列反压链路接通
4. 不再依赖本地 UDP 环路传递数据
5. Git 提交：`git commit -m "feat(02-02): 合并单进程架构并接通反压"`
</verification>

<success_criteria>
1. `wfb_core` 作为单进程入口存在
2. 线程角色与共享状态定义明确
3. TUN 队列与动态水位线形成反压闭环
4. 控制面使用 `poll()` 驱动唤醒
5. 不再依赖本地 UDP 环路
6. 代码符合 Google C++ Style Guide
7. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-architecture/02-02-SUMMARY.md`
</output>
