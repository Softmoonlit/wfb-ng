---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [src/threads.h, src/wfb_core.cpp, src/tx.hpp, src/tx.cpp]
autonomous: true
requirements: [PROC-05, PROC-07, PROC-10, PROC-11]
must_haves:
  truths:
    - "发射端线程必须用 condition_variable 唤醒"
    - "发射端线程必须检查 can_send 和 token_expire_time_ms"
    - "唤醒后应立即释放锁进入发射准备"
  artifacts:
    - path: "src/threads.h"
      provides: "发射线程声明与共享状态"
      contains: ["tx_main_loop", "can_send"]
    - path: "src/wfb_core.cpp"
      provides: "发射线程接线"
      contains: "condition_variable"
    - path: "src/tx.cpp"
      provides: "发射接口复用"
      contains: "send_packet"
  key_links:
    - from: "TUN 读取线程"
      to: "cv_send.notify_one()"
      via: "装弹通知"
      pattern: "notify_one"
    - from: "发射线程"
      to: "can_send"
      via: "逻辑阀门"
      pattern: "can_send"
---

<objective>
实现空口发射线程的唤醒与门控逻辑，让发射路径从轮询改为条件变量驱动，形成零轮询唤醒链路。

Purpose: 降低发射延迟毛刺，确保缺弹时沉睡、装弹后立即开火。
Output: 发射线程骨架、条件变量唤醒和共享状态门控。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-process-architecture/PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

# 现有代码接口
From src/tx.hpp:
```cpp
bool send_packet(const uint8_t *buf, size_t size, uint8_t flags, bool bypass_fec = false);
```

From src/tx.cpp:
```cpp
bool Transmitter::send_packet(const uint8_t *buf, size_t size, uint8_t flags, bool bypass_fec)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义发射线程共享状态</name>
  <files>src/threads.h</files>
  <behavior>
    - Test 1: can_send、token_expire_time_ms、cv_send 可被发射线程引用
    - Test 2: 共享状态边界清晰
    - Test 3: 不与控制面状态混淆
  </behavior>
  <action>
定义发射线程需要的共享状态和同步对象。

1. 声明 `can_send`
2. 声明 `token_expire_time_ms`
3. 声明 `std::condition_variable cv_send`
4. 保持状态粒度最小
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/threads.h -o /tmp/test_threads.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>发射线程共享状态就绪</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现发射端线程唤醒与锁释放</name>
  <files>src/wfb_core.cpp, src/threads.h</files>
  <behavior>
    - Test 1: `cv_send.wait()` 可唤醒发射线程
    - Test 2: 唤醒后尽快释放锁
    - Test 3: 发射线程逻辑不依赖 usleep()
  </behavior>
  <action>
建立发射线程主循环的条件变量等待逻辑。

1. 使用 `cv_send.wait()` 或等价写法
2. 唤醒后立即释放锁进入检查流程
3. 不在发射路径中引入固定轮询延迟
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>发射端线程转为零轮询唤醒</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 固化 can_send 与过期时间门控</name>
  <files>src/wfb_core.cpp</files>
  <behavior>
    - Test 1: 发射前检查 can_send
    - Test 2: 发射前检查 token_expire_time_ms
    - Test 3: 任一条件不满足时不发射并关闭阀门
  </behavior>
  <action>
把发射条件固定为逻辑阀门，避免缺弹或过期 token 触发错误发射。

1. 检查 `can_send`
2. 检查 `token_expire_time_ms`
3. 不满足条件时将 `can_send` 置为 false
  </action>
  <verify>
    <automated>g++ -std=c++11 -c src/wfb_core.cpp -o /tmp/test_wfb_core.o 2>&1 | grep -i error || echo "编译成功"</automated>
  </verify>
  <done>发射逻辑由逻辑阀门严格控制</done>
</task>

</tasks>

<verification>
1. 编译测试通过：`g++ -std=c++11 -c src/wfb_core.cpp`
2. 发射线程使用 `condition_variable`，不再依赖 `usleep()`
3. `can_send` 和 `token_expire_time_ms` 门控有效
4. Git 提交：`git commit -m "feat(02-03): 实现发射线程零轮询唤醒"`
</verification>

<success_criteria>
1. 发射端线程存在并可被唤醒
2. 发射路径不使用固定轮询延迟
3. `can_send` 门控生效
4. 过期 token 不会触发错误发射
5. 代码符合 Google C++ Style Guide
6. 所有函数和关键逻辑添加中文注释
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-architecture/02-03-SUMMARY.md`
</output>
