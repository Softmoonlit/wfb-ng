---
phase: 03-server-scheduler
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified: [src/server_scheduler.h, src/server_scheduler.cpp, tests/test_scheduler.cpp]
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08]

must_haves:
  truths:
    - "三态状态机正确实现状态 I/II/III 跃迁"
    - "Watchdog 保护生效，连续 3 次静默才判定断流"
    - "状态 II 使用 max(MIN, actual + MARGIN) 公式"
    - "状态 III 使用 min(MAX, allocated + STEP) 公式"
    - "MAX_WINDOW 根据节点数动态计算"
  artifacts:
    - path: "src/server_scheduler.h"
      provides: "三态状态机接口"
      contains: "NodeStateEnum"
    - path: "src/server_scheduler.cpp"
      provides: "状态机实现"
      contains: "calculate_next_window"
    - path: "tests/test_scheduler.cpp"
      provides: "状态机测试"
      contains: "test_state_machine"
  key_links:
    - from: "src/server_scheduler.cpp"
      to: "src/aq_sq_manager.h"
      via: "AqSqManager"
      pattern: "aq_sq_manager"
---

<objective>
扩展服务端三态状态机，实现完整的状态跃迁逻辑、Watchdog 保护和动态 MAX_WINDOW 计算。

Purpose: 根据客户端实际使用时间动态调整授权窗口，平衡吞吐量和公平性。
Output: 完整的三态状态机引擎，支持节点状态追踪和队列迁移。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-scheduler/03-RESEARCH.md

# 已有基础设施
@src/server_scheduler.h
@src/server_scheduler.cpp

# 依赖的 Phase 3 计划
@.planning/phases/03-server-scheduler/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 NodeState 结构和状态枚举</name>
  <files>src/server_scheduler.h</files>
  <read_first>
    - /home/kilome/wfb-ng/src/server_scheduler.h
    - /home/kilome/wfb-ng/src/aq_sq_manager.h
  </read_first>
  <action>
扩展 ServerScheduler 头文件，添加以下内容：

1. 添加状态枚举（per SCHED-01）:
```cpp
enum class NodeStateEnum {
    STATE_I,   // 枯竭与睡眠 (Zero-Payload Drop, < 5ms)
    STATE_II,  // 唤醒与需量拟合 (Demand-Fitting, 5ms <= t < allocated)
    STATE_III  // 高负载贪突发 (Additive Increase, t >= allocated)
};
```

2. 扩展 NodeState 结构:
```cpp
struct NodeState {
    uint8_t silence_count = 0;        // Watchdog 计数器（per SCHED-02）
    NodeStateEnum current_state = NodeStateEnum::STATE_I;  // 当前状态
    uint16_t current_window = 50;     // 当前授权窗口
};
```

3. 添加 AqSqManager 依赖:
```cpp
#include "aq_sq_manager.h"

class ServerScheduler {
private:
    // ... existing members ...
    AqSqManager* aq_sq_manager = nullptr;  // 队列管理器引用
    size_t num_nodes = 10;                  // 节点数量

public:
    void set_aq_sq_manager(AqSqManager* manager);
    void set_num_nodes(size_t n);
    uint16_t get_max_window() const;  // 动态计算 MAX_WINDOW
};
```

4. 更新 calculate_next_window 方法签名（无需修改，保持现有接口）

关键约束:
- MIN_WINDOW = 50ms（已有，per SCHED-05）
- STEP_UP = 100ms（已有，per SCHED-07）
- ELASTIC_MARGIN = 50ms（已有，per SCHED-08）
- MAX_WINDOW 需要动态计算（per SCHED-06）
  </action>
  <verify>
    <automated>grep -E "(enum class NodeStateEnum|STATE_I|STATE_II|STATE_III)" /home/kilome/wfb-ng/src/server_scheduler.h</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `enum class NodeStateEnum` 定义
    - 包含 `STATE_I`, `STATE_II`, `STATE_III` 枚举值
    - NodeState 结构包含 `current_state` 成员
    - NodeState 结构包含 `current_window` 成员
    - 包含 `AqSqManager*` 成员变量
    - 包含 `get_max_window()` 方法声明
  </acceptance_criteria>
  <done>
状态枚举和扩展结构定义完成，接口更新就绪。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现三态状态机核心逻辑</name>
  <files>src/server_scheduler.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/src/server_scheduler.cpp
    - /home/kilome/wfb-ng/src/server_scheduler.h
    - /home/kilome/wfb-ng/src/aq_sq_manager.h
  </read_first>
  <action>
重写 calculate_next_window 方法，实现完整的三态状态机逻辑：

1. 状态 I：极速枯竭与睡眠 (Zero-Payload Drop, < 5ms):
```cpp
if (actual_used < 5) {
    state.silence_count++;
    if (state.silence_count >= 3) {
        // 连续 3 次静默，判定断流（per SCHED-02）
        state.current_state = NodeStateEnum::STATE_I;
        state.current_window = MIN_WINDOW;
        // 迁移到 SQ
        if (aq_sq_manager) {
            aq_sq_manager->migrate_to_sq(node_id);
        }
        return MIN_WINDOW;
    }
    // Watchdog 保护：保持原窗口
    return allocated;
}
```

2. 有活动，重置静默计数:
```cpp
state.silence_count = 0;
```

3. 状态 III：高负载贪突发 (Additive Increase, >= allocated):
```cpp
if (actual_used >= allocated) {
    state.current_state = NodeStateEnum::STATE_III;
    uint16_t target = allocated + STEP_UP;
    state.current_window = std::min(target, get_max_window());
    // 确保在 AQ 中
    if (aq_sq_manager) {
        aq_sq_manager->migrate_to_aq(node_id);
    }
    return state.current_window;
}
```

4. 状态 II：需量拟合 (Demand-Fitting, 5ms <= t < allocated):
```cpp
state.current_state = NodeStateEnum::STATE_II;
uint16_t target = actual_used + ELASTIC_MARGIN;
state.current_window = std::max(target, MIN_WINDOW);
// 迁移到 AQ
if (aq_sq_manager) {
    aq_sq_manager->migrate_to_aq(node_id);
}
return state.current_window;
```

5. 实现 get_max_window()（per SCHED-06）:
```cpp
uint16_t ServerScheduler::get_max_window() const {
    // 确保 (N-1) × MAX_WINDOW < UFTP GRTT (假设 5000ms)
    if (num_nodes <= 5) return 600;   // 5 节点及以下
    if (num_nodes <= 10) return 300;  // 10 节点及以下
    return 150;                        // 更多节点进一步减小
}
```

关键约束:
- Watchdog: 连续 3 次静默才判定断流（per SCHED-02）
- 状态 II 公式: max(MIN, actual + MARGIN)（per SCHED-03）
- 状态 III 公式: min(MAX, allocated + STEP)（per SCHED-04）
- MAX_WINDOW 动态计算（per SCHED-06）
  </action>
  <verify>
    <automated>grep -E "(silence_count|STATE_I|STATE_II|STATE_III|get_max_window)" /home/kilome/wfb-ng/src/server_scheduler.cpp</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `silence_count` 相关逻辑
    - 包含 Watchdog 检查（`silence_count >= 3`）
    - 包含状态 III 扩窗逻辑（`allocated + STEP_UP`）
    - 包含状态 II 需量拟合逻辑（`actual_used + ELASTIC_MARGIN`）
    - 包含 `get_max_window()` 实现
    - 包含 AQ/SQ 迁移调用
  </acceptance_criteria>
  <done>
三态状态机核心逻辑实现完成，状态跃迁正确，集成 AQ/SQ 迁移。
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展状态机单元测试</name>
  <files>tests/test_scheduler.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/tests/test_scheduler.cpp
    - /home/kilome/wfb-ng/src/server_scheduler.h
  </read_first>
  <action>
扩展现有测试文件，添加以下测试用例：

1. `test_state_machine_initial_state`:
   - 验证新节点初始状态为 STATE_I
   - 验证初始窗口为 MIN_WINDOW

2. `test_state_iii_additive_increase`:
   - 节点耗尽 100ms 授权窗口
   - 验证下一窗口 = min(100 + 100, MAX_WINDOW)
   - 验证状态跃迁到 STATE_III

3. `test_state_ii_demand_fitting`:
   - 节点使用 60ms（小于 100ms 授权）
   - 验证下一窗口 = max(50, 60 + 50) = 110ms
   - 验证状态跃迁到 STATE_II

4. `test_watchdog_protection`:
   - 节点连续 2 次静默（< 5ms）
   - 验证窗口保持不变（Watchdog 保护）
   - 第 3 次静默，验证窗口降级到 MIN_WINDOW

5. `test_max_window_dynamic`:
   - 设置 5 节点，验证 MAX_WINDOW = 600ms
   - 设置 10 节点，验证 MAX_WINDOW = 300ms
   - 设置 20 节点，验证 MAX_WINDOW = 150ms

6. `test_state_transition_sequence`:
   - 模拟完整的状态跃迁序列：
     - 初始 STATE_I → 状态 II（首次活动）
     - 状态 II → 状态 III（耗尽窗口）
     - 状态 III → 状态 I（连续 3 次静默）

测试框架: 使用 assert 简单测试（保持现有风格）
  </action>
  <verify>
    <automated>./tests/test_scheduler 2>/dev/null || echo "Test needs compilation"</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `test_state_iii_additive_increase` 测试函数
    - 包含 `test_state_ii_demand_fitting` 测试函数
    - 包含 `test_watchdog_protection` 测试函数
    - 包含 `test_max_window_dynamic` 测试函数
    - main() 函数调用所有新增测试
    - 测试覆盖 STATE_I/II/III 三种状态
  </acceptance_criteria>
  <done>
状态机测试完整覆盖所有状态跃迁场景，编译运行通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 状态机输入 | actual_used 和 allocated 来自外部反馈，需验证范围 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | actual_used 参数 | mitigate | 验证 actual_used <= allocated |
| T-03-02-02 | Tampering | node_id 参数 | mitigate | 验证节点 ID 范围 1-10 |
</threat_model>

<verification>
1. 编译项目: `make clean && make`
2. 运行测试: `./tests/test_scheduler`
3. 验证状态枚举: `grep -E "STATE_I|STATE_II|STATE_III" src/server_scheduler.h`
4. 验证 Watchdog: `grep "silence_count >= 3" src/server_scheduler.cpp`
5. 验证公式: `grep -E "STEP_UP|ELASTIC_MARGIN" src/server_scheduler.cpp`
</verification>

<success_criteria>
1. 三态状态机正确实现状态 I/II/III 跃迁（SCHED-01）
2. Watchdog 保护生效，连续 3 次静默才判定断流（SCHED-02）
3. 状态 II 使用 max(MIN, actual + MARGIN) 公式（SCHED-03）
4. 状态 III 使用 min(MAX, allocated + STEP) 公式（SCHED-04）
5. MAX_WINDOW 根据节点数动态计算（SCHED-06）
6. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-scheduler/03-02-SUMMARY.md`
</output>
