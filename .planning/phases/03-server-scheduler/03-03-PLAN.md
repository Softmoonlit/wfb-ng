---
phase: 03-server-scheduler
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/breathing_cycle.h, src/breathing_cycle.cpp, tests/test_breathing.cpp]
autonomous: true
requirements: [SCHED-12]

must_haves:
  truths:
    - "呼吸周期在呼出和吸气状态之间正确切换"
    - "呼出阶段持续 800ms，吸气阶段持续 200ms"
    - "吸气阶段依次为每个节点分配 NACK 窗口"
    - "节点切换时插入保护间隔"
  artifacts:
    - path: "src/breathing_cycle.h"
      provides: "呼吸周期接口"
      contains: "BreathingCycle"
    - path: "src/breathing_cycle.cpp"
      provides: "呼吸周期实现"
      contains: "update"
    - path: "tests/test_breathing.cpp"
      provides: "呼吸周期测试"
      contains: "test_breathing"
  key_links:
    - from: "src/breathing_cycle.cpp"
      to: "src/guard_interval.h"
      via: "apply_guard_interval"
      pattern: "apply_guard_interval"
---

<objective>
实现下行广播"呼吸"周期，在呼出阶段全速下发模型，吸气阶段分配 NACK 回传窗口。

Purpose: 实现服务端下行广播的时间分片机制，平衡数据下发和反馈收集。
Output: 呼吸周期状态机，支持精确的时间控制和阶段切换。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-scheduler/03-RESEARCH.md

# 已有基础设施
@src/guard_interval.h
@src/guard_interval.cpp
@src/threads.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建呼吸周期状态机接口</name>
  <files>src/breathing_cycle.h</files>
  <read_first>
    - /home/kilome/wfb-ng/src/guard_interval.h
    - /home/kilome/wfb-ng/src/threads.h
  </read_first>
  <action>
创建呼吸周期状态机头文件，定义以下接口：

1. 相位枚举（per SCHED-12）:
```cpp
enum class BreathingPhase {
    EXHALE,   // 呼出：服务器全速下发 40MB 模型
    INHALE    // 吸气：服务器暂停，分配 NACK 回传窗口
};
```

2. 呼吸周期类:
```cpp
class BreathingCycle {
private:
    BreathingPhase current_phase = BreathingPhase::EXHALE;
    uint64_t phase_start_ms = 0;         // 当前相位开始时间
    size_t num_nodes = 10;               // 节点数量

    // 时间常量（per SCHED-12）
    static constexpr uint16_t EXHALE_DURATION_MS = 800;  // 呼出 800ms
    static constexpr uint16_t INHALE_DURATION_MS = 200;  // 吸气 200ms
    static constexpr uint16_t NACK_WINDOW_MS = 25;       // NACK 窗口 25ms/节点
    static constexpr uint16_t GUARD_INTERVAL_MS = 5;     // 保护间隔 5ms

public:
    // 更新呼吸周期状态，返回当前相位
    BreathingPhase update(uint64_t current_time_ms);

    // 执行吸气阶段：为每个节点分配 NACK 窗口
    void execute_inhale_phase();

    // 获取当前相位
    BreathingPhase get_current_phase() const;

    // 设置节点数量
    void set_num_nodes(size_t n);

    // 获取周期总时长
    uint32_t get_cycle_duration_ms() const;
};
```

关键设计:
- 使用 CLOCK_MONOTONIC 时间源
- 呼出/吸气相位严格按时间切换
- 吸气阶段为每个节点预留 NACK 窗口
  </action>
  <verify>
    <automated>grep -E "(enum class BreathingPhase|class BreathingCycle|EXHALE|INHALE)" /home/kilome/wfb-ng/src/breathing_cycle.h</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `enum class BreathingPhase` 定义
    - 包含 `EXHALE` 和 `INHALE` 枚举值
    - 包含 `class BreathingCycle` 定义
    - 包含 `EXHALE_DURATION_MS = 800` 常量
    - 包含 `INHALE_DURATION_MS = 200` 常量
    - 包含 `NACK_WINDOW_MS = 25` 常量
    - 包含 `update()` 方法声明
    - 包含 `execute_inhale_phase()` 方法声明
  </acceptance_criteria>
  <done>
呼吸周期接口定义完成，包含所有时间常量和方法声明。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现呼吸周期状态机逻辑</name>
  <files>src/breathing_cycle.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/src/breathing_cycle.h
    - /home/kilome/wfb-ng/src/guard_interval.cpp
  </read_first>
  <action>
实现呼吸周期状态机核心逻辑：

1. `update(current_time_ms)` 实现（per RESEARCH.md Pattern 3）:
```cpp
BreathingPhase BreathingCycle::update(uint64_t current_time_ms) {
    // 初始化：首次调用记录开始时间
    if (phase_start_ms == 0) {
        phase_start_ms = current_time_ms;
    }

    uint64_t elapsed = current_time_ms - phase_start_ms;

    if (current_phase == BreathingPhase::EXHALE && elapsed >= EXHALE_DURATION_MS) {
        // 呼出结束，切换到吸气
        current_phase = BreathingPhase::INHALE;
        phase_start_ms = current_time_ms;
        return BreathingPhase::INHALE;
    }
    else if (current_phase == BreathingPhase::INHALE && elapsed >= INHALE_DURATION_MS) {
        // 吸气结束，切换到呼出
        current_phase = BreathingPhase::EXHALE;
        phase_start_ms = current_time_ms;
        return BreathingPhase::EXHALE;
    }

    return current_phase;
}
```

2. `execute_inhale_phase()` 实现:
```cpp
void BreathingCycle::execute_inhale_phase() {
    // 吸气阶段逻辑（伪代码框架）：
    // 1. 暂停下行数据发送
    // 2. 依次为每个节点分配 NACK 窗口

    for (size_t i = 0; i < num_nodes; i++) {
        uint8_t node_id = static_cast<uint8_t>(i + 1);

        // 分配 NACK 窗口（此处为框架实现）
        // 实际实现需要集成 Token 发射器
        // emitter.send_token(node_id, NACK_WINDOW_MS);

        // 保护间隔（per SCHED-12, GUARD_INTERVAL = 5ms）
        apply_guard_interval(GUARD_INTERVAL_MS);
    }
}
```

3. 辅助方法实现:
```cpp
BreathingPhase BreathingCycle::get_current_phase() const {
    return current_phase;
}

void BreathingCycle::set_num_nodes(size_t n) {
    num_nodes = n;
}

uint32_t BreathingCycle::get_cycle_duration_ms() const {
    return EXHALE_DURATION_MS + INHALE_DURATION_MS;  // 1000ms
}
```

关键约束:
- 呼出 800ms，吸气 200ms，总计 1 秒周期
- NACK 窗口 25ms/节点
- 节点切换保护间隔 5ms
- 使用 apply_guard_interval() 实现高精度保护间隔
  </action>
  <verify>
    <automated>grep -E "(update|execute_inhale_phase|EXHALE_DURATION_MS|INHALE_DURATION_MS|NACK_WINDOW_MS)" /home/kilome/wfb-ng/src/breathing_cycle.cpp</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `update()` 实现，处理 EXHALE→INHALE 切换
    - 包含 `update()` 实现，处理 INHALE→EXHALE 切换
    - 包含 `execute_inhale_phase()` 实现
    - 包含 `apply_guard_interval(GUARD_INTERVAL_MS)` 调用
    - EXHALE_DURATION_MS = 800
    - INHALE_DURATION_MS = 200
    - NACK_WINDOW_MS = 25
  </acceptance_criteria>
  <done>
呼吸周期状态机实现完成，支持精确的相位切换和吸气阶段执行。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建呼吸周期单元测试</name>
  <files>tests/test_breathing.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/src/breathing_cycle.h
    - /home/kilome/wfb-ng/tests/test_scheduler.cpp
  </read_first>
  <action>
创建呼吸周期单元测试，覆盖以下场景：

1. `test_initial_phase`:
   - 创建 BreathingCycle
   - 验证初始相位为 EXHALE

2. `test_exhale_to_inhale_transition`:
   - 创建 BreathingCycle
   - 调用 update() 模拟时间流逝
   - 验证 800ms 后切换到 INHALE

3. `test_inhale_to_exhale_transition`:
   - 创建 BreathingCycle
   - 先切换到 INHALE
   - 验证 200ms 后切换回 EXHALE

4. `test_cycle_duration`:
   - 创建 BreathingCycle
   - 验证 get_cycle_duration_ms() == 1000

5. `test_full_cycle`:
   - 模拟完整的呼吸周期（EXHALE→INHALE→EXHALE）
   - 验证每个相位持续正确时间

6. `test_inhale_nack_windows`:
   - 创建 BreathingCycle，设置 10 节点
   - 调用 execute_inhale_phase()
   - 验证执行时间约为 10 * (25 + 5) = 300ms（含保护间隔）

测试框架: 使用 assert 简单测试（与现有风格一致）
  </action>
  <verify>
    <automated>./tests/test_breathing 2>/dev/null || echo "Test needs compilation"</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `test_initial_phase` 测试函数
    - 包含 `test_exhale_to_inhale_transition` 测试函数
    - 包含 `test_inhale_to_exhale_transition` 测试函数
    - 包含 `test_cycle_duration` 测试函数
    - 包含 `test_full_cycle` 测试函数
    - main() 函数调用所有测试
    - 包含 `#include "../src/breathing_cycle.h"`
  </acceptance_criteria>
  <done>
呼吸周期测试完整覆盖相位切换和时间常量，编译运行通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 时间输入 | current_time_ms 来自系统时钟，可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | current_time_ms | accept | 来自 CLOCK_MONOTONIC，不可被用户篡改 |
</threat_model>

<verification>
1. 编译项目: `make clean && make`
2. 运行测试: `./tests/test_breathing`
3. 验证接口: `grep -E "class BreathingCycle" src/breathing_cycle.h`
4. 验证常量: `grep -E "EXHALE_DURATION_MS|INHALE_DURATION_MS" src/breathing_cycle.cpp`
5. 验证保护间隔: `grep "apply_guard_interval" src/breathing_cycle.cpp`
</verification>

<success_criteria>
1. 呼吸周期状态机正确实现（SCHED-12）
2. 呼出/吸气相位按时切换（800ms/200ms）
3. 吸气阶段 NACK 窗口分配正确（25ms/节点）
4. 保护间隔正确插入（5ms）
5. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-scheduler/03-03-SUMMARY.md`
</output>
