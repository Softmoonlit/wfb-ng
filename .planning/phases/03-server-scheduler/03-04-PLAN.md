---
phase: 03-server-scheduler
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: [src/server_scheduler.h, src/server_scheduler.cpp, tests/test_scheduler.cpp, tests/test_aq_sq.cpp]
autonomous: true
requirements: [SCHED-09, SCHED-13, SCHED-14]

must_haves:
  truths:
    - "空闲巡逻模式在 AQ 为空时单节点探询 SQ"
    - "探询间隔为 100ms（IDLE_PATROL_INTERVAL）"
    - "混合交织调度在 AQ 和 SQ 间正确选择节点"
    - "高精度定时使用 timerfd + 短自旋"
    - "调度器主线程提权至 SCHED_FIFO"
  artifacts:
    - path: "src/server_scheduler.h"
      provides: "调度器集成接口"
      contains: "IDLE_PATROL_INTERVAL"
    - path: "src/server_scheduler.cpp"
      provides: "调度器集成实现"
      contains: "get_next_node_to_serve"
    - path: "tests/test_scheduler.cpp"
      provides: "调度器测试"
      contains: "test_idle_patrol"
    - path: "tests/test_aq_sq.cpp"
      provides: "队列测试扩展"
      contains: "test_interleave_integration"
  key_links:
    - from: "src/server_scheduler.cpp"
      to: "src/aq_sq_manager.h"
      via: "get_next_node"
      pattern: "get_next_node"
    - from: "src/server_scheduler.cpp"
      to: "src/guard_interval.h"
      via: "apply_guard_interval"
      pattern: "apply_guard_interval"
---

<objective>
集成空闲巡逻模式和混合交织调度，完成服务端调度引擎的最后组装。

Purpose: 实现高效的节点选择策略，区分活跃节点和睡眠节点的服务频率。
Output: 完整的调度引擎，支持空闲巡逻、混合交织和实时调度提权。
</objective>

<execution_context>
@D:/Code/wfb-ng/.claude/get-shit-done/workflows/execute-plan.md
@D:/Code/wfb-ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-scheduler/03-RESEARCH.md

# 已有基础设施
@src/server_scheduler.h
@src/server_scheduler.cpp
@src/guard_interval.h
@src/threads.h

# 依赖的 Phase 3 计划
@.planning/phases/03-server-scheduler/03-01-PLAN.md
@.planning/phases/03-server-scheduler/03-02-PLAN.md
@.planning/phases/03-server-scheduler/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加空闲巡逻常量和接口</name>
  <files>src/server_scheduler.h</files>
  <read_first>
    - /home/kilome/wfb-ng/src/server_scheduler.h
    - /home/kilome/wfb-ng/src/threads.h
  </read_first>
  <action>
扩展 ServerScheduler 头文件，添加空闲巡逻相关常量和方法：

1. 添加空闲巡逻常量（per SCHED-09）:
```cpp
// 空闲巡逻常量（per SCHED-09）
static constexpr uint16_t IDLE_PATROL_INTERVAL_MS = 100;  // 空闲巡逻间隔
static constexpr uint16_t MICRO_PROBE_DURATION_MS = 15;   // 微探询时长

// 混合交织常量（per SCHED-10）
static constexpr int INTERLEAVE_RATIO = 4;  // 每 4 次 AQ 服务穿插 1 次 SQ
```

2. 添加调度接口:
```cpp
// 获取下一个要服务的节点
// 返回值: pair<node_id, duration_ms>
// duration_ms: 授权窗口时长（AQ 节点使用 current_window，SQ 节点使用 MICRO_PROBE_DURATION）
std::pair<uint8_t, uint16_t> get_next_node_to_serve();

// 执行调度周期
void execute_scheduling_cycle();

// 更新节点状态（收到客户端反馈后调用）
void update_node_state(uint8_t node_id, uint16_t actual_used, uint16_t allocated);
```

3. 添加时间追踪:
```cpp
private:
    uint64_t last_patrol_time_ms = 0;  // 上次巡逻时间
```

关键约束:
- IDLE_PATROL_INTERVAL = 100ms（per SCHED-09）
- MICRO_PROBE_DURATION = 15ms（覆盖调度抖动 + 网卡延迟）
- INTERLEAVE_RATIO = 4（介于 3-5 之间，per SCHED-10）
  </action>
  <verify>
    <automated>grep -E "(IDLE_PATROL_INTERVAL|MICRO_PROBE_DURATION|INTERLEAVE_RATIO|get_next_node_to_serve)" /home/kilome/wfb-ng/src/server_scheduler.h</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `IDLE_PATROL_INTERVAL_MS = 100` 常量
    - 包含 `MICRO_PROBE_DURATION_MS = 15` 常量
    - 包含 `INTERLEAVE_RATIO = 4` 常量
    - 包含 `get_next_node_to_serve()` 方法声明
    - 包含 `execute_scheduling_cycle()` 方法声明
    - 包含 `update_node_state()` 方法声明
  </acceptance_criteria>
  <done>
空闲巡逻常量和接口定义完成。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现空闲巡逻和混合交织调度</name>
  <files>src/server_scheduler.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/src/server_scheduler.cpp
    - /home/kilome/wfb-ng/src/aq_sq_manager.h
    - /home/kilome/wfb-ng/src/guard_interval.h
  </read_first>
  <action>
实现空闲巡逻和混合交织调度核心逻辑：

1. `get_next_node_to_serve()` 实现:
```cpp
std::pair<uint8_t, uint16_t> ServerScheduler::get_next_node_to_serve() {
    if (!aq_sq_manager) {
        return {0xFF, 0};  // 无效节点
    }

    // 获取下一个节点（混合交织调度）
    auto [node_id, is_from_sq] = aq_sq_manager->get_next_node(INTERLEAVE_RATIO);

    if (node_id == 0xFF) {
        return {0xFF, 0};  // 无节点可服务
    }

    uint16_t duration_ms;
    if (is_from_sq) {
        // SQ 节点：使用微探询时长（per SCHED-09）
        duration_ms = MICRO_PROBE_DURATION_MS;
    } else {
        // AQ 节点：使用当前授权窗口
        auto it = nodes.find(node_id);
        if (it != nodes.end()) {
            duration_ms = it->second.current_window;
        } else {
            duration_ms = MIN_WINDOW;
        }
    }

    return {node_id, duration_ms};
}
```

2. `execute_scheduling_cycle()` 实现（框架）:
```cpp
void ServerScheduler::execute_scheduling_cycle() {
    // 1. 获取下一个要服务的节点
    auto [node_id, duration_ms] = get_next_node_to_serve();

    if (node_id == 0xFF) {
        return;  // 无节点
    }

    // 2. 发射 Token（需要集成 TokenEmitter）
    // token_emitter->send_token_triple(transmitter, node_id, duration_ms);

    // 3. 插入保护间隔（per SCHED-13, 使用 timerfd + 短自旋）
    apply_guard_interval(kGuardIntervalMs);
}
```

3. `update_node_state()` 实现:
```cpp
void ServerScheduler::update_node_state(uint8_t node_id, uint16_t actual_used, uint16_t allocated) {
    // 调用 calculate_next_window 更新状态
    uint16_t next_window = calculate_next_window(node_id, actual_used, allocated);

    // 日志记录（可选）
    // printf("Node %d: actual=%d, allocated=%d, next=%d\n",
    //        node_id, actual_used, allocated, next_window);
}
```

关键约束:
- 空闲巡逻：AQ 为空时，100ms 间隔探询单个 SQ 节点
- 混合交织：每 N 次 AQ 服务穿插 1 次 SQ 探询
- 保护间隔：节点切换时插入 5ms（复用 guard_interval）
  </action>
  <verify>
    <automated>grep -E "(get_next_node_to_serve|execute_scheduling_cycle|update_node_state|MICRO_PROBE_DURATION|INTERLEAVE_RATIO)" /home/kilome/wfb-ng/src/server_scheduler.cpp</automated>
  </verify>
  <acceptance_criteria>
    - 包含 `get_next_node_to_serve()` 实现
    - 包含对 `aq_sq_manager->get_next_node()` 的调用
    - SQ 节点使用 `MICRO_PROBE_DURATION_MS`
    - AQ 节点使用 `current_window`
    - 包含 `execute_scheduling_cycle()` 实现
    - 包含 `apply_guard_interval()` 调用
    - 包含 `update_node_state()` 实现
  </acceptance_criteria>
  <done>
空闲巡逻和混合交织调度实现完成，集成保护间隔。
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展测试覆盖空闲巡逻和混合交织</name>
  <files>tests/test_scheduler.cpp, tests/test_aq_sq.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/tests/test_scheduler.cpp
    - /home/kilome/wfb-ng/tests/test_aq_sq.cpp
    - /home/kilome/wfb-ng/src/server_scheduler.h
  </read_first>
  <action>
扩展测试文件，添加以下测试用例：

**tests/test_scheduler.cpp 新增测试：**

1. `test_idle_patrol_constants`:
   - 验证 IDLE_PATROL_INTERVAL_MS == 100
   - 验证 MICRO_PROBE_DURATION_MS == 15
   - 验证 INTERLEAVE_RATIO == 4

2. `test_get_next_node_to_serve`:
   - 创建 ServerScheduler 和 AqSqManager
   - 初始化节点 1, 2 到 AQ
   - 调用 get_next_node_to_serve()
   - 验证返回节点在 AQ 中
   - 验证 duration_ms 使用 current_window

3. `test_micro_probe_for_sq`:
   - 创建 ServerScheduler 和 AqSqManager
   - 初始化节点 1 到 SQ（AQ 为空）
   - 调用 get_next_node_to_serve()
   - 验证 duration_ms == MICRO_PROBE_DURATION_MS

**tests/test_aq_sq.cpp 新增测试：**

4. `test_interleave_integration`:
   - 创建 AqSqManager
   - 初始化 4 节点到 AQ，1 节点到 SQ
   - 连续调用 get_next_node(4) 10 次
   - 统计 SQ 服务次数，验证约为 2 次（10/5）

测试框架: 使用 assert 简单测试
  </action>
  <verify>
    <automated>./tests/test_scheduler && ./tests/test_aq_sq 2>/dev/null || echo "Tests need compilation"</automated>
  </verify>
  <acceptance_criteria>
    - test_scheduler.cpp 包含 `test_idle_patrol_constants` 测试
    - test_scheduler.cpp 包含 `test_get_next_node_to_serve` 测试
    - test_scheduler.cpp 包含 `test_micro_probe_for_sq` 测试
    - test_aq_sq.cpp 包含 `test_interleave_integration` 测试
    - 所有测试函数在 main() 中调用
  </acceptance_criteria>
  <done>
测试完整覆盖空闲巡逻和混合交织调度场景，编译运行通过。
  </done>
</task>

<task type="auto">
  <name>Task 4: 验证高精度定时和实时调度提权</name>
  <files>tests/test_guard_interval.cpp, tests/test_threads.cpp</files>
  <read_first>
    - /home/kilome/wfb-ng/src/guard_interval.cpp
    - /home/kilome/wfb-ng/src/threads.h
    - /home/kilome/wfb-ng/tests/test_threads.cpp
  </read_first>
  <action>
验证 Phase 2 已实现的高精度定时和实时调度提权（SCHED-13, SCHED-14）：

1. 高精度定时验证（SCHED-13）:
   - 确认 guard_interval.cpp 使用 timerfd + 短自旋实现
   - 确认精度目标 < 100μs
   - 运行测试: `./tests/test_guard_interval` 或编译验证

2. 实时调度提权验证（SCHED-14）:
   - 确认 threads.h 定义了:
     - kControlThreadPriority = 99
     - kTxThreadPriority = 90
   - 确认 set_thread_realtime_priority() 函数存在
   - 确认 check_root_permission() 函数存在
   - 运行测试: `./tests/test_threads`

3. 如果测试文件不存在，创建简单的验证测试:
```cpp
// tests/test_guard_interval.cpp（如不存在）
#include <cassert>
#include "../src/guard_interval.h"

void test_guard_interval_exists() {
    // 验证函数可调用
    apply_guard_interval(5);
}

int main() {
    test_guard_interval_exists();
    return 0;
}
```

验证命令:
- 编译: `make clean && make`
- 测试: `./tests/test_threads && ./tests/test_guard_interval`
  </action>
  <verify>
    <automated>grep -E "(kControlThreadPriority|kTxThreadPriority|set_thread_realtime_priority|apply_guard_interval)" /home/kilome/wfb-ng/src/threads.h /home/kilome/wfb-ng/src/guard_interval.h 2>/dev/null | head -10</automated>
  </verify>
  <acceptance_criteria>
    - threads.h 包含 `kControlThreadPriority = 99`
    - threads.h 包含 `kTxThreadPriority = 90`
    - threads.h 包含 `set_thread_realtime_priority()` 声明
    - guard_interval.h 包含 `apply_guard_interval()` 声明
    - 编译成功，测试通过
  </acceptance_criteria>
  <done>
高精度定时和实时调度提权验证完成，Phase 2 基础设施就绪。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调度器内部 | 所有输入来自内部状态机，无外部不可信输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | 空闲巡逻循环 | accept | 固定 100ms 间隔，无 CPU 占用风险 |
</threat_model>

<verification>
1. 编译项目: `make clean && make`
2. 运行所有测试: `make test` 或逐个运行
3. 验证常量: `grep -E "IDLE_PATROL_INTERVAL|INTERLEAVE_RATIO" src/server_scheduler.h`
4. 验证调度: `grep "get_next_node_to_serve" src/server_scheduler.cpp`
5. 验证保护间隔: `grep "apply_guard_interval" src/server_scheduler.cpp`
</verification>

<success_criteria>
1. 空闲巡逻模式正确实现（SCHED-09）
   - AQ 为空时 100ms 间隔探询单节点
   - SQ 节点使用 MICRO_PROBE_DURATION（15ms）
2. 混合交织调度正确集成（SCHED-10）
   - 每 4 次 AQ 服务穿插 1 次 SQ 探询
3. 高精度定时验证通过（SCHED-13）
   - timerfd + 短自旋实现
4. 实时调度提权验证通过（SCHED-14）
   - 控制面 99，发射端 90
5. 所有单元测试通过
6. 代码通过编译，无警告
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-scheduler/03-04-SUMMARY.md`
</output>
